# Codebase Patterns

Seeded from the design session on 2026-08-17. These are verified against source; trust them.

## Build and test commands
- Build (skip ITs): `./gradlew build -x :integ-test:integTest`
- Unit tests: `./gradlew test`
- All integration tests: `./gradlew :integ-test:integTest`
- Single IT class: `./gradlew :integ-test:integTest --tests "org.opensearch.sql.calcite.remote.CalciteDedupCommandIT"`
- Single IT method: `./gradlew :integ-test:integTest --tests "org.opensearch.sql.calcite.remote.CalciteExplainIT.testComplexDedup"`
- `-Dtests.security.manager=false` is already set in build.gradle.
- `:doctest:bootstrap` CANNOT run on this dev host: the repo venv is python3.7 and `doctest/sql-cli` pins `boto3==1.34.34`, which requires python>=3.8. `./gradlew build -x :integ-test:integTest` therefore FAILS on `:doctest:bootstrap` for reasons unrelated to any code change. Use `./gradlew build -x :integ-test:integTest -x :doctest:bootstrap -x :doctest:doctest` instead.
- Full Java build takes ~9 min cold; `./gradlew test` ~20s warm; `./gradlew spotlessCheck` ~35s.

## Settings that already exist — do not add new ones
- `plugins.calcite.enabled` → CALCITE_ENGINE_ENABLED
- `plugins.calcite.pushdown.enabled` → CALCITE_PUSHDOWN_ENABLED
- `plugins.calcite.fallback.allowed` → CALCITE_FALLBACK_ALLOWED
- Defined in `common/src/main/java/org/opensearch/sql/common/setting/Settings.java:42-47`
- The PoC posture is `pushdown.enabled=false` and `fallback.allowed=false`. This is a config flip, not new code.

## Plan-level limit already exists — do not add a second limit concept
- `QueryService.convertToCalcitePlan` (`core/src/main/java/org/opensearch/sql/executor/QueryService.java:1032-1036`) wraps every plan in `LogicalSystemLimit(QUERY_SIZE_LIMIT, querySizeLimit())`.
- `LogicalSystemLimit extends Sort`, so it has a `fetch` and is optimizer-visible.
- It is already matched by `LimitIndexScanRule` (`opensearch/src/main/java/org/opensearch/sql/opensearch/planner/rules/LimitIndexScanRule.java:24`).
- `plugins.query.size_limit` (`OpenSearchSettings.java:204`) falls back to `IndexSettings.MAX_RESULT_WINDOW_SETTING` = 10000.

## Explain goldens
- YAML files under `integ-test/src/test/resources/expectedOutput/calcite/`
- Loaded via `loadExpectedPlan("name.yaml")` at `PPLIntegTestCase:484`, asserted with `assertEquals`
- Re-baselining means editing the YAML
- THE DIRECTORY IS CHOSEN BY A CLUSTER-SETTING READ, and this bounds the blast radius of every staged-path output change: `loadExpectedPlan` (`PPLIntegTestCase:484`) calls `isPushdownDisabled()` (line 398), which reads the TRANSIENT `plugins.calcite.pushdown.enabled`. Enabled -> `expectedOutput/calcite/` (372 files). Disabled -> `expectedOutput/calcite_no_pushdown/` (167 files). Every `Staged*IT` class sets that setting false in `init()`, so staged output is asserted ONLY against `calcite_no_pushdown/`. An empty `git diff -- expectedOutput/calcite/` is therefore the load-bearing proof that a staged change left the production default untouched — quote it in every story that touches explain output.
- RE-BASELINING NEEDS A MECHANICAL ADDITIONS-ONLY GATE, declared before you start. A +/- line count is not a gate and a summary claim like "just trailing-newline normalization" has already been wrong once (US-011: 74 files had deletions, and one was a real `SEARCH($10, Sarg[[30..40]])` -> `<($10, 40)` plan change). The check: `git diff -U0 -- <goldens> | grep '^-' | grep -v '^---'` must contain only lines that are re-added identically modulo an appended trailing comma or an added end-of-file newline, and every changed `.json` golden must PARSE to an object equal to HEAD's minus exactly the intended new keys. Any other deletion is a regression until proven otherwise.
- New explain fields are backward-compatible FOR FREE: `YamlFormatter` sets `JsonInclude.Include.NON_NULL` (`core/.../utils/YamlFormatter.java:32`) and the JSON path uses Gson, which omits nulls by default and never calls `serializeNulls()`. A field left null is invisible in output, so a staged-only section needs NO explain-mode gate and NO new setting.
- BUT adding a field to `ExplainResponseNodeV2` also requires updating `ExplainResponse.normalizeLf` (`core/.../executor/ExecutionEngine.java:144`): it rebuilds the node through the 3-arg constructor, so any field not explicitly re-set there is silently DROPPED. Same defect family as the Writeable round-trip checklist.
- A test that asserts a POSTURE must not live in a class that a `Staged*IT` subclasses, or the subclass inherits it under the opposite posture and fails. Put both directions in a standalone `PPLIntegTestCase` subclass with an `@After` restoring the settings to null. Precedent: `CalciteStageSplitExplainIT`. Assert on the YAML KEY (`"combine:"`), never the bare word.

## OpenSearch aggregation lifecycle constraints
- `InternalAggregation.reduce()` is called in batches of `batched_reduce_size` (default 512) by `QueryPhaseResultConsumer.PendingReduces`. It MUST be associative. There is no opt-out.
- `mustReduceOnSingleInternalAgg()` only forces reduce() when a single shard returned; it does NOT disable batching.
- Therefore the wire type must carry UNFINALIZED accumulator state (avg ships {sum, count}, never an average).
- Batched reduce lowers peak memory only when the combine reduces volume. CONCAT gains nothing.
- `Aggregator.collect()` can read _source via `SearchContext.lookup().getLeafSearchLookup(ctx).source()`, and this works under `size: 0` because it is independent of the fetch phase.
- `size: 0` sets `hasTopDocs=false`, skipping the fetch phase, so no reader context and no PIT is created.
- `CollectionTerminatedException` terminates the CURRENT LEAF ONLY — it is caught by `ContextIndexSearcher#searchLeaf()` and the leaf loop continues. Stopping a whole shard needs a shard-global counter plus a guard in `getLeafCollector()`.
- `allow_partial_search_results` defaults to TRUE. Every request this PoC builds must set `allowPartialSearchResults(false)`, or a budget breach returns HTTP 200 with partial results.
- Transport ceiling from data node to coordinator is 30% of JVM heap, hardcoded in `TcpTransport`. There is no `transport.max_message_size` setting.

## Calcite facts
- Aggregate splitting bottoms out per function: `SqlSplittableAggFunction` with `CountSplitter`, `SumSplitter`, `Sum0Splitter`, `SelfSplitter`. If `unwrap(SqlSplittableAggFunction.class)` returns null the call cannot be split.
- There is no generic mechanism to split Window, Sort, Join, or Distinct. The standard fallback is gather to SINGLETON.
- Limit-pushability is per-operator: exact below Project/Calc, merges with Sort into top-N, exact per-branch for Union ALL. NOT below Filter, Aggregate, general Window, Join, or Union DISTINCT.
- PPL `dedup` already lowers to `ROW_NUMBER() OVER (PARTITION BY ...)` plus a `<= k` filter — no custom operator is involved on the Calcite path.

## Custom logical operators (audit result, US-001)
- Only three custom logical RelNodes exist on the Calcite path:
  - `LogicalSystemLimit` (`core/.../calcite/plan/rel/LogicalSystemLimit.java:25`, a `Sort` subclass, present in every plan)
  - `LogicalDedup` (`core/.../calcite/plan/rel/LogicalDedup.java:20`, transient during HEP, lowered by `PPLDedupConvertRule`)
  - `LogicalGraphLookup` (`core/.../calcite/plan/rel/LogicalGraphLookup.java:22`, irreducible, from `graphLookup`, becomes `CalciteEnumerableGraphLookup`)
- `TimeWindow` (`core/.../calcite/plan/TimeWindow.java:15`) is dead code never instantiated.
- Every other PPL command produces only standard Calcite operators.
- `dedup` uses `ROW_NUMBER` exclusively (never `RANK`/`DENSE_RANK`) via `CalciteRelNodeVisitor.visitDedupe()` (line 2208) and `PPLDedupConvertRule.buildDedupNotNull()`/`buildDedupOrNull()`.
- `dedup consecutive=true` throws `CalciteUnsupportedException` (`CalciteRelNodeVisitor.java:2217`).

## Staged execution wiring (US-008, COMPLETE)
- Hook: `OpenSearchExecutionEngine.execute(RelNode, CalcitePlanContext, ResponseListener)` in `opensearch`. NOT `core/QueryService` — `core` cannot see `opensearch`.
- Gate: `StagePlan.staged() && plugins.calcite.pushdown.enabled == false`, read via `stagePlan.shardScan().getOsIndex().getSettings()`. No new setting. When pushdown is enabled (the production default) nothing changes.
- Gathered-rows row type is `stagePlan.shardFragment().getRowType()`, NEVER `shardScan().getRowType()`. They differ whenever a shard-local Project/Filter sits above the scan, and using the scan's type silently reads wrong column indices.
- `OpenSearchClient.search(OpenSearchRequest)` parses the raw `InternalAggregation` into `ExprValue` iterators and so CANNOT return the real `InternalCalciteExec`. Use `client.getNodeClient().get().search(searchRequest).actionGet()`. Staged execution is therefore NodeClient-only.
- `allowPartialSearchResults` is on `SearchRequest`, NOT `SearchSourceBuilder` (invariant 7).
- Tier 2 is `CoordinatorTreeExecutor` — same HEP + Volcano + `toBindable` + custom `DataContext` recipe as the shard side, wrapped in `CalciteClassLoaderHelper.withCalciteClassLoader`. It needs the FULL enumerable rule set, not just `ENUMERABLE_TABLE_SCAN_RULE`, because `LogicalSystemLimit` (a `Sort`) lands on the coordinator as a logical node.
- STILL OPEN (deliberately, none of them blocking): `buildStagedSearchRequest` is public static only so a test in another package can reach it; the coordinator `DataContext` returns null for `UTC_TIMESTAMP`; the `OpenSearchRestClient` path throws `IllegalStateException("Staged execution requires a NodeClient")`; `CoordinatorTreeExecutor.execute`'s `gatheredRowType` parameter is redundant because the coordinatorTree's leaf already carries its row type.

## The staged response must derive ExprType from the plan, not from the Java value
- The staged row-conversion loop originally called the single-arg `ExprValueUtils.fromObjectValue(Object)`, which dispatches on the RUNTIME CLASS. Shard rows carry temporal values as epoch-millis `Long`, so a date came back as `1420070400000` while the JDBC path returns `"2015-01-01 00:00:00"`. Dispatching on the runtime class can never be right here: the wire representation is lossy about intent.
- Fix pattern, reusable for any future staged output type: pre-compute `ExprType[]` ONCE from `coordinatorTree.getRowType()` outside the row loop via `OpenSearchTypeFactory.convertRelDataTypeToExprType`, pass it to the two-arg `fromObjectValue(Object, ExprType)`, and have the schema-building loop reuse the SAME array. Values and declared schema then cannot diverge by construction.
- `ExprValueUtils.fromObjectValue(Object, ExprType)` lives in `core` and is shared. It already converts `Long` -> TIMESTAMP/DATE/TIME via `Instant.ofEpochMilli`; it now also returns `LITERAL_NULL` for a null input instead of NPE-ing.
- Any staged query projecting a temporal column exercises this. Assert against the JDBC path's exact string form, including the sub-second-nanos variant (`"1970-01-19 08:31:22.955"`), not just whole seconds — the formatter branches on `getNano() == 0`.

## Nested fields on the staged shard path: expand with sibling alignment
- OpenSearch `nested` sub-documents are SEPARATE HIDDEN LUCENE DOCUMENTS. The parent has no doc_values for a nested leaf, and `_source` extraction of `address.city` on the parent returns a FLATTENED List of every sub-document's value. A Calcite filter comparing that List to a scalar is always false — 0 rows, no error. This is the single most dangerous shape on this path because it is silent.
- The wire `FieldDescriptor` type for `address.city` is the LEAF type (`text`), NOT `nested`, so type-based dispatch alone can never detect the situation. Detection must come from the MAPPING.
- Nested paths are resolved on the SHARD, not shipped: `CalciteExecAggregator.findNestedPath` walks the dotted path from the leaf upward asking `SearchContext.getQueryShardContext().getObjectMapper(parent)` whether it is nested; the FIRST hit walking upward is the innermost path. This deliberately avoids touching the wire format — prefer shard-side mapping resolution over widening `FieldDescriptor`, because asymmetric Writeable round-trip is this codebase's most invisible defect class.
- `ShardRowReader.readRows(doc)` returns `List<Object[]>`: one output row per nested sub-document. Any field with a non-null nested path is read from `_source` (never doc_values). `readRow(doc)` is unchanged and still serves the non-nested case, so non-nested queries are byte-for-byte unaffected.
- SIBLING ALIGNMENT IS THE LOAD-BEARING PROPERTY: group requested fields BY nested path, expand each distinct path's sub-document array ONCE, and read all fields of that path from the SAME sub-document. Expanding per FIELD independently would cross-product siblings and be silently wrong. A test for this must use MULTIPLE sub-documents and assert the pairing — with one sub-document, a correct implementation and a cross-producting one are indistinguishable.
- Fields under DIFFERENT nested paths DO cross-product. Documented in a code comment and in the story notes; not silent.
- Absent vs malformed must be separated: a nested array genuinely MISSING from `_source` is a legitimately sparse parent and yields one row with null nested values (correct — a downstream filter drops it). A value PRESENT with an unexpected shape THROWS, naming the nested path, the field, and the actual runtime class. Collapsing both into "return nulls" reintroduces the silent-zero-row bug being fixed.
- Row cardinality now exceeds document count for nested queries, so `rowsCollected` counts POST-expansion buffered rows. US-009's row budget must be written with that in mind.

## Prove a "pre-existing failure" by stashing, do not assert it
- A parent IT that fails both before and after a change is only credible if you SHOW it: `git stash push`, re-run the single test, read the XML, `git stash pop`. `CalciteWhereCommandIT.testScriptFilterOnDifferentNestedHierarchyShouldThrow` was verified this way. Cheap, and it removes the most common way a real regression gets waved through — especially when the change touches shared `core` code that could alter an exception type the test asserts on.


## Separating staged-path defects from posture limitations
- When a staged IT fails, do NOT reason about whether the cause is staging or `pushdown.enabled=false`. Neutralize ONLY the gate (`if (false && ...)`) and re-run the same IT classes — they set the settings themselves, so the run isolates the posture. This one experiment invalidated a whole round of plausible-but-wrong gap analysis in US-008.

## Integration tests share ONE cluster
- `integTest` runs every IT class against a single cluster, so a transient cluster setting applied in `init()` leaks into every class that runs afterwards, in non-deterministic order. Always restore to `null` in teardown. Precedent: `integ-test/.../remote/CalcitePPLAggregationPaginatingIT.java:18-22`.

## Delegating code review
- `reviewer` missed one real defect per round in US-003/005/007 while catching only style items, then caught BOTH criticals in US-008. The difference was the prompt: name the project's historical defect shapes explicitly (sentinel/placeholder bypass, silent catch-and-degrade fallback, test whose NAME does not match what its BODY constructs, comment asserting an unenforced rule) and require a hunted-each-one checklist in the output. Do this every round.

## Design invariants — do not violate
1. The split must be TOTAL over RelNode. Absence of an optimization rule means the operator runs on the coordinator. Never reject a plan.
2. Never emit a Painless script query. If a predicate is not index-accelerable, leave it as a residual Filter in the fragment.
3. Never silently truncate. Either the limit is provably pushable, or the budget refuses the query with a diagnosable error.
4. One search request per OpenSearchIndexScan. Never create a PIT, scroll, or search_after on this path.
5. Standard Calcite operators only.

Full design: `docs/dev/poc-staged-calcite-exec-design.md`

## The stage split: StagePlanner (US-007)
- `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/StagePlanner.java` — `static StagePlan split(RelNode root)`. Never throws, never returns null, for any RelNode tree.
- MODULE DEVIATION, and it is permanent: the design doc assigns StagePlanner to `core`, but `core/build.gradle` has NO dependency on `:opensearch` while StagePlanner must reference `CombineDescriptor`, `RelFragmentCodec`, and `AbstractCalciteIndexScan` — all in `opensearch`. It therefore lives in `opensearch`. Dependency direction is `opensearch --api--> core`, one way only. Any future story that the doc places in `core` but which touches the stage package has the same problem.
- Shard-scan recognition is the single package-private predicate `StagePlanner.isShardScan(RelNode)` → `node instanceof AbstractCalciteIndexScan`. Widen THERE, nowhere else. Do NOT make every `TableScan` SHARD_LOCAL — that would falsely claim non-OpenSearch tables are shard-distributed.
- Placement is one memoized bottom-up pass into an `IdentityHashMap<RelNode, Boolean>`, then a top-down walk reading the memo to find the highest SHARD_LOCAL node. Use IDENTITY maps for RelNode keys throughout — `AbstractRelNode` does not override `equals`/`hashCode`, so `equals` is reference equality today, but relying on that implicitly is fragile.
- A `Project` or `Calc` containing a `RexOver` is NEEDS_GATHER, not SHARD_LOCAL. PPL lowers window functions (`dedup`, `eventstats`) into a `LogicalProject` holding `ROW_NUMBER() OVER (...)`, and per-shard numbering restarts at 1, so shipping it shard-local and CONCAT-ing is SILENTLY WRONG. Guards: `RexOver.containsOver(project.getProjects(), null)` and `RexOver.containsOver(calc.getProgram())`. A `Filter` needs no such guard — the Calcite 1.42.0 validator rejects a `RexOver` in a filter condition. Partial window evaluation is US-015's `RANK_LIMIT` rule and is the ONLY correct way to make windows shard-local.
- The shardFragment's leaf is NOT the `AbstractCalciteIndexScan`. `split()` replaces it with a plain `LogicalTableScan` over `[RelFragmentCodec.SCHEMA_NAME, <indexName>]` carrying the SAME row type, so `RelFragmentCodec.serialize(shardFragment)` round-trips. The original scan is preserved on `StagePlan.shardScan()` so a later story can read index name, pushed-down DSL, and field list without re-walking the tree.
- The coordinatorTree replaces the whole cut subtree with a gathered-rows `LogicalTableScan` whose row type is the CUT NODE's row type, backed by `RelFragmentCodec.ShardRowSourceTable` parameterized with `StagePlanner.GATHERED_ROWS_STASH_KEY` = `"calcite_exec.gathered_rows"` (table name `_gathered_rows`). `ShardRowSourceTable` now has a 2-arg ctor taking a stash key; the 1-arg ctor still defaults to `SHARD_ROWS_STASH_KEY`, so the US-005 shard path is untouched.
- ZERO or MORE THAN ONE `AbstractCalciteIndexScan` (e.g. a join across two indices) has no single gather boundary, so `split()` returns a coordinator-only `StagePlan`: `staged()==false`, `coordinatorTree()==root` untouched, `shardFragment()`/`shardScan()`/`combine()` null. This preserves Design Invariant 1 (never reject a plan). Multi-scan staging is deliberately out of PoC scope. Callers MUST check `staged()`.
- `LogicalSystemLimit` is a `Sort` subclass, so the generic floor makes it NEEDS_GATHER and it lands on the coordinator. That is correct for the floor; US-013/US-014 own limit pushdown. Do not special-case it.
- Tree rebuilds use `node.copy(node.getTraitSet(), newInputs)`. Safe for `LogicalSystemLimit`, Project, Filter, Calc because the replacement leaf's row type is identical to the original's, so no `RexInputRef` shifts index or type. Preserve row type exactly whenever swapping a leaf.

## Custom aggregation: calcite_exec (US-002)
- Aggregation type name: `calcite_exec`
- Package: `org.opensearch.sql.opensearch.stage` (in `opensearch` module)
- Classes:
  - `CombineDescriptor` — enum of reduce modes (CONCAT, MERGE_AGG, LIMIT, TOP_N, RANK_LIMIT) plus parameters
  - `CalciteExecAggregationBuilder` — XContent parser + Writeable builder; fields: plan, fields, combine, rowBudget
  - `CalciteExecAggregatorFactory` — creates aggregator via `createInternal(SearchContext, Aggregator, CardinalityUpperBound, Map<String,Object>)`
  - `CalciteExecAggregator` — extends MetricsAggregator; collects docs, executes fragment, emits InternalCalciteExec
  - `InternalCalciteExec` — InternalAggregation; carries rows + combine descriptor; reduce() applies combine mode
- Registration: `SQLPlugin.getAggregations()` (SQLPlugin now implements SearchPlugin)
- Module-visibility rule: `opensearch` module has the full opensearch server jar (`opensearch/build.gradle:36`); `core` has only `opensearch-core`. Any class touching InternalAggregation, AggregatorFactory, MetricsAggregator, or SearchContext must live in `opensearch` (or `plugin`), never `core`.
- CombineDescriptor modes: CONCAT (US-002), MERGE_AGG (US-012), LIMIT (US-013), TOP_N (US-014), RANK_LIMIT (US-015)

## Fragment codec: RelFragmentCodec (US-003)
- `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/RelFragmentCodec.java` — `serialize(RelNode) -> base64 RelJson` and `deserialize(base64, indexName, List<FieldDescriptor>) -> RelNode`.
- Calcite 1.42.0 signatures that make custom RelJson injection possible: `RelJsonWriter(JsonBuilder, UnaryOperator<RelJson>)` and `RelJsonReader(RelOptCluster, RelOptSchema, Schema, UnaryOperator<RelJson>)`. Use these to inject `ExtendedRelJson` so PPL operators round-trip. Plain `RelJson` would silently lose them.
- `RelJsonSerializer.getPplSqlOperatorTable()` is now `public static` (backing field is `volatile`, so the double-checked-locking singleton is correct). ALWAYS reuse it — never re-chain PPLBuiltinOperators + SqlStdOperatorTable + OperatorTable + SqlLibrary by hand.
- `ExtendedRelJson.create(jsonBuilder)` leaves `inputTranslator = null`, and `withOperatorTable()` NPEs on a null translator because the 3-arg ctor `requireNonNull`s it. So on the read path you MUST chain `.withInputTranslator(...)` before `.withOperatorTable(...)`. The translator itself is never invoked — `RelJsonReader` resolves input refs through its own `RelInput` — so it should throw `UnsupportedOperationException` rather than fabricate refs. An earlier draft fabricated `SqlTypeName.ANY` refs, which would have been silently wrong if ever reached.
- Table path convention is `[OpenSearch, <indexName>]` (`RelFragmentCodec.SCHEMA_NAME`). The SAME path must be registered on both the serialization and the deserialization side or `RelOptUtil.toString()` will not match.
- `RelFragmentCodec.SHARD_ROWS_STASH_KEY` = `"calcite_exec.shard_rows"`. `ShardRowSourceTable` is a `ScannableTable` that reads `List<Object[]>` from `DataContext.get(SHARD_ROWS_STASH_KEY)` and returns an empty enumerable when absent. US-005 injects rows here.

## Wire type strings are OPENSEARCH type names, not SqlTypeName names
- `CalciteExecAggregationBuilder.FieldDescriptor.type` is a free-form `String` carrying an OpenSearch field type. The authoritative example at `docs/dev/poc-staged-calcite-exec-design.md:270-272` is `{"name":"account_number","type":"long"}` / `{"name":"gender","type":"keyword"}`.
- Therefore `SqlTypeName.valueOf(fd.getType())` ALWAYS throws on real wire data. `RelFragmentCodec` owns the mapping: keyword/text→VARCHAR, long→BIGINT, integer→INTEGER, double→DOUBLE, float→REAL, boolean→BOOLEAN, date→TIMESTAMP; anything else throws naming the field and the type. Extend that map when a story needs a new type — do not add a second mapping elsewhere.
- Do NOT reuse `OpenSearchTypeFactory.convertExprTypeToRelDataType` for this: it takes an `ExprType` object rather than a string, and returns UDT wrapper types (ExprDateType, ExprTimeStampType, …) for date/time/timestamp/ip that do not survive the RelJson round-trip.
- Lesson: write unit tests against the REAL wire spellings from the design doc. The first draft's tests fed `"BIGINT"`/`"VARCHAR"` and passed green while the production path was broken for every actual caller.

## Shard row materialization: ShardRowReader (US-004)
- `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/ShardRowReader.java` owns ALL field-value extraction for the shard fragment. One `FieldReader` closure per requested field, built once per Lucene leaf in `CalciteExecAggregator.getLeafCollector()`; `readRow(doc)` returns an `Object[]` with exactly one slot per requested field, in request order, `null` when the field is absent from a doc.
- Type dispatch keys off the SAME OpenSearch type strings as the wire (`keyword`, `long`, `integer`, `double`, `float`, `boolean`, `date`, `text`) — see the wire-type note above. Keep this switch and `RelFragmentCodec`'s SqlTypeName map in sync: adding a type needs BOTH.
- doc_values access uses `advanceExact(doc)`, never `advance()`. `advanceExact` is stateful per reader instance, so a reader MUST be created per leaf — never cache a `FieldReader` across `getLeafCollector()` calls or docIDs from a later leaf will silently miss.
- Encoding decisions in the Object[]: keyword → `String` (BytesRef.utf8ToString), long/date → `Long` (date is epoch millis), integer → `Integer`, double → `Double` via `NumericUtils.sortableLongToDouble`, float → `Float` via `sortableIntToFloat`, boolean → `Boolean` from 0/1. Numeric doc_values are stored sortable-encoded — reading the raw long for a double/float gives garbage.
- `text` (and any field with no doc_values) falls back to `_source`. `ShardRowReader.create()` does NOT position the `SourceLookup`; the CALLER must call `sourceLookup.setSegmentAndDocument(ctx, doc)` before `readRow(doc)`. Obtain the lookup via `SearchContext.lookup().getLeafSearchLookup(ctx).source()` — this works under `size: 0` because it is independent of the fetch phase.
- `InternalCalciteExec` carries `rowsCollected` and `rowsEmitted` as VLongs (both in `writeTo`/StreamInput ctor/XContent/equals/hashCode). They are currently always equal; they diverge only once US-009 enforces the row budget.

## Shard fragment execution: the compile-and-run pipeline (US-005)
- `CalciteExecAggregator.buildAggregation()` is the single execution entry point on the shard. One code path only: `RelFragmentCodec.deserialize` -> HEP pass -> Volcano pass -> Janino `Bindable` -> `bind(DataContext)` -> drain. There is deliberately NO bypass, no sentinel plan value, and no catch-and-return-raw-rows fallback. A bad plan MUST fail loudly. Do not reintroduce one to keep an older test green — fix the test instead.
- Reaching a `Bindable` needs TWO optimizer passes, not one:
  1. HEP: `PROJECT_TO_LOGICAL_PROJECT_AND_WINDOW`, `FILTER_TO_CALC`, `PROJECT_TO_CALC`, `CALC_MERGE`.
  2. Volcano: convert to `EnumerableConvention`, then `EnumerableInterpretable.toBindable(ImmutableMap.of(), null, rel, Prefer.ARRAY)`.
  Why the HEP pass is mandatory: `EnumerableProject.implement()` throws `UnsupportedOperationException` under `Prefer.ARRAY`, so Projects must become Calcs; and a `RexOver` sitting inside a `LogicalProject` NEVER becomes an `EnumerableWindow` unless `ProjectToWindowRule` extracts it first. A single Volcano pass silently fails on both.
- The `SparkHandler` argument to `toBindable` is `@Nullable` and `null` is the CORRECT value — that is precisely the branch that Janino-compiles. Passing a handler would route to Spark.
- `CalciteClassLoaderHelper.withCalciteClassLoader(Callable, callerClass)` (in `core`, visible from `opensearch`) MUST wrap the whole compile+bind+drain block. Janino resolves classes through `Thread.currentThread().getContextClassLoader()` (CALCITE-3745); without the wrapper the generated code cannot see plugin classes on a data node. The helper restores the loader in a `finally`.
- `DataContexts.of(Map)` is NOT usable here: generated `EnumerableTableScan` code calls `root.getRootSchema().getSubSchema("OpenSearch").getTable(...)` at runtime and `DataContexts.of` leaves `rootSchema` null. Use a custom `DataContext` serving BOTH the root schema AND the `SHARD_ROWS_STASH_KEY` slot. It currently returns null for `DataContext.Variable.UTC_TIMESTAMP` — a fragment containing `NOW()`/`CURRENT_TIMESTAMP` will NPE in `UserDefinedFunctionUtils.restoreFunctionProperties:168`. Whichever story first ships a temporal fragment must fix that, and must verify the unit (millis vs nanos) against Calcite 1.42 rather than guessing.
- `Prefer.ARRAY` returns a BARE SCALAR, not an `Object[]`, when the output row type has exactly one field. Any drain loop needs that guard or single-column fragments blow up on the cast.
- The row buffer is `List<Object[]>` (matching `ShardRowReader.readRow` and the stash contract), never `List<List<Object>>`.
- `RelFragmentCodec`'s deserialization planner registers `ConventionTraitDef.INSTANCE` and `RelCollationTraitDef.INSTANCE`. Both are required for the EnumerableConvention conversion; a cluster without them cannot be optimized at all.
- `executeFragment(...)` is package-private static precisely so unit tests can drive compile-and-run with a literal `List<Object[]>` and no aggregation framework. Prefer that over an IT when testing plan shapes.
- Do NOT route shard execution through a JDBC `Connection`/`RelRunner` (`CalciteToolsHelper.OpenSearchRelRunners`). That is the coordinator path; the data node compiles a `Bindable` directly.

## Pushdown and PredicateAnalyzer
- `PredicateAnalyzer` lives at `opensearch/src/main/java/org/opensearch/sql/opensearch/request/PredicateAnalyzer.java` and is a `RexVisitorImpl<QueryBuilder>`. Entry points: `analyze(RexNode)` (returns `QueryBuilder` or throws) and `analyzeExpression(RexNode)` (same, no script fallback).
- The predicate path can NO LONGER emit a Painless script. The script-query emission path (`ScriptQueryExpression` in `Visitor.tryAnalyzeOperand` and the `catch`-and-wrap in `analyzeExpression`) is deleted as of US-006.
- Pushdown refusal is signalled by `PredicateAnalyzerException` or `ExpressionNotAnalyzableException`. This is SAFE because:
  - `FilterIndexScanRule` only calls `call.transformTo()` on a non-null result from `PredicateAnalyzer` — a thrown exception means the Filter stays intact in the Calcite plan.
  - `AggregateIndexScanRule` only calls `call.transformTo()` when `CalciteLogicalIndexScan.pushDownAggregate()` returns non-null; that method catches `PredicateAnalyzerException`, logs at debug, and returns null.
- DANGER: nested-field aggregations REQUIRE pushdown and have no Calcite fallback. If a filter on a nested-field aggregation is refused by `PredicateAnalyzer`, the query fails with HTTP 500 PLANNING_ERROR. Any future narrowing of pushability MUST run `CalciteExplainIT` (all 266 tests) as a regression gate.
- The only remaining `new ScriptQueryExpression` in main is in `AggregateAnalyzer` (~line 187, for aggregation MEASURE scripts) and the sort path (`SortAnalyzer`). Neither is a predicate.
- Explain goldens live under `integ-test/src/test/resources/expectedOutput/calcite/` and are re-baselined by editing the YAML/JSON files directly.

## Row budget and circuit breaker on the shard (US-009)
- Budget breach is a HARD exception: `RowBudgetExceededException extends OpenSearchException`, thrown from `CalciteExecAggregator.collect()` when `rowsCollected > rowBudget` (checked AFTER buffering, so exactly-budget succeeds). Message: `<forcingOperator> requires all rows on one node; gathered N rows, budget M` with `%,d` grouping; subject is the literal `"this query"` when nothing forced a gather.
- The design doc's OTHER throw site — `CollectionTerminatedException` + a `getLeafCollector()` guard — is ONLY for an exactly-pushable limit (US-013 rule 4). Never use it for the budget: it is caught per-leaf by `ContextIndexSearcher#searchLeaf()`, so it silently truncates and violates Design Invariant 3.
- A `Writeable` OpenSearch exception does NOT need exception-registry registration for its MESSAGE to reach the REST body. The type is lost across the transport, the message survives. Assert on message TEXT in ITs, never on the exception class name.
- `addRequestCircuitBreakerBytes(long)` is inherited protected from `AggregatorBase` (`AggregatorBase.java:169`). Release is AUTOMATIC in `AggregatorBase.close()` via `addWithoutBreaking(-requestBytesUsed)` — do not hand-release, but confirm no subclass overrides `close()`/`doClose()` without calling super. Charge in batches of 1024 (OpenSearch's `MultiBucketConsumer` probe cadence), never per row, and flush the final partial batch on EVERY exit path including the throw.
- `forcingOperator` is `RelNode.getRelTypeName()` computed by `StagePlanner` as the immediate parent of the cut (null when the cut IS the root), with a `[window]` qualifier appended for a Project/Calc holding a `RexOver` — otherwise the message blames a `LogicalProject`, which normally IS shard-local, and misleads. The qualifier reuses the single `StagePlanner.containsWindowFunction(RelNode)` that the placement pass uses, so classification and message cannot drift. Do NOT reverse-map RelNode classes to PPL command names like `eventstats`; that is not soundly derivable and the doc's example is illustrative of message SHAPE only.

## Shipping a coordinator-only fact to the shard: the wire-widening checklist
- Ask FIRST whether the shard can derive the fact from its own mapping/context. US-008's nested paths could (~15 lines, zero round-trip risk); US-009's forcing operator could not, because only the coordinator holds the plan.
- When you must ship it, widening `CalciteExecAggregationBuilder` requires ALL of: `writeTo`, the `StreamInput` ctor (same ORDER, same optional-ness), XContent output, the XContent PARSER, `equals`, `hashCode`, setter/getter, and a round-trip test asserting WHOLE-OBJECT equality for both the null and non-null case (`CalciteExecAggregationBuilderWireTest` is that test — extend it). A field missing from only the parser, or only `equals`, is completely invisible on a single-node test cluster.

## Raw `_search` integration tests must put `allow_partial_search_results=false` ON THE URL
- It defaults to TRUE. Without it, a shard-level exception returns HTTP 200 with a `_shards.failed` entry and partial results, so a fail-fast test passes by not throwing while proving nothing. The staged path sets it programmatically on `SearchRequest`, but a hand-built REST request in an IT does not inherit that.
- `TEST_INDEX_BANK` is 7 docs on a single shard, which makes small row budgets deterministic (`row_budget: 2` breaches at `gathered 3 rows`). That determinism is what lets an IT assert the EXACT message substring instead of a loose `contains("rows")`.

## Trusting test numbers: archive the XML, and read the skip messages
- `integTest` DELETES the whole `integ-test/build/test-results/integTest/` directory at the start of every invocation. Running classes in batches therefore destroys the earlier batches' XML, and any number reported from a wiped batch is unverifiable. Run every class in ONE invocation, or copy the XML out of `build/` immediately (`.sisyphus/handoff/<story>-xml/`). Combine with the existing rule: always `--rerun-tasks` and read counts from the XML, never from the Gradle exit code.
- A green class with a high skip count is a FINDING. Read the skip MESSAGES: `CalciteExplainIT` self-skips 76 of 266 tests via `assumeTrue("This test is only for when push down is enabled")`, so under the PoC posture it reports 0 failures while actually covering 186/266. Distinguish posture-induced assumption skips from inherited parent `@Ignore`s (e.g. `org.opensearch.sql.ppl.HeadCommandIT`, issue 703) before quoting any pass rate.
- When a story's deliverable is a TABLE of counts, machine-check it: every row must satisfy `tests - skipped == pass + fail-on-assertion + fail-on-ceiling`, and the per-cause annotation counts must sum to the failure total. A hand-summed footer is a defect site — the US-010 pass total was transcribed 332 for a true 333.
- When a bucket comes back EMPTY, state why in the report. `0 fail-on-ceiling` means nothing approached the budget, NOT that the ceiling works.

## Landing a measurement story without turning CI red
- A measurement story adds test classes that are EXPECTED to fail; the failures are the deliverable. Committing them red breaks the shared branch, deleting them destroys the reproducible harness. Resolution: commit them all, and put a class-level `@Ignore` on exactly the red ones whose reason states the measured failure count, the actual exception shapes, and the story ID that unblocks it. The unblocking story deletes one annotation as part of its acceptance. Precedent: `StagedCalcite{Top,Stats,Dedup}CommandIT` and `StagedCalcitePPLEventstatsIT` (US-010, unblocked by US-012/US-014/US-015).
- The posture-setting IT subclass pattern is: extend the parent, override `init()` to set `CALCITE_PUSHDOWN_ENABLED=false` and `CALCITE_FALLBACK_ALLOWED=false` as `transient` cluster settings, and `@After tearDown()` restoring BOTH to `null` before `super.tearDown()`. The restore is load-bearing — integTest shares one cluster. Canonical example: `integ-test/src/test/java/org/opensearch/sql/calcite/remote/StagedCalciteWhereCommandIT.java`.
- The design doc's per-class test counts are STALE (`stats` is 63 not 4, `top` is 9 not 5). Never gate on a count taken from the doc.

# Iteration Log

<!-- Round summaries and human intervention notes will be appended here automatically -->

## 2026-08-17 - US-001
- Audited every PPL command in `ppl/src/main/antlr/OpenSearchPPLParser.g4` for custom (non-standard) logical RelNodes on the Calcite path and recorded the inventory in the design doc. Documentation-only change; no production code touched.
- Files changed: `docs/dev/poc-staged-calcite-exec-design.md` (new `## Custom logical operator audit (US-001)` section, 31 lines, inserted before `## Worked example 1`), `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Tenet 6 does NOT hold today. `LogicalGraphLookup` is an irreducible custom `BiRel` from the `graphLookup` command; it survives optimization and converts directly to `CalciteEnumerableGraphLookup`. US-007's StagePlanner must place it as `NEEDS_GATHER` — which the generic placement floor already does by default, so this is a documentation point, not a blocker.
  - `LogicalSystemLimit` is in EVERY plan, so any StagePlanner placement function must handle it explicitly or it will force a gather on every query. It is a `Sort` subclass with `fetch` only and no collation.
  - `LogicalDedup` exists but is transient: it appears only mid-HEP and `PPLDedupConvertRule` lowers it back to `LogicalFilter` + `LogicalProject`(ROW_NUMBER window). Do not build split logic against `LogicalDedup` — build it against the window + rank-filter shape, which is what US-015 already specifies.
  - `dedup consecutive=true` is unsupported (throws `CalciteUnsupportedException`), so US-015 needs no consecutive variant and no golden exists for one.
  - `:doctest:bootstrap` is permanently broken on this host (python3.7 vs `boto3==1.34.34` needing >=3.8). Always build with `-x :doctest:bootstrap -x :doctest:doctest` or every round will report a false build failure.
  - Handoff research notes from this round: `.sisyphus/handoff/explorer-custom-relnodes.md`, `.sisyphus/handoff/explorer-dedup-golden.md`
---

## 2026-08-17 - US-002
- Registered the `calcite_exec` custom aggregation skeleton. New package `org.opensearch.sql.opensearch.stage` in the `opensearch` module with CombineDescriptor, CalciteExecAggregationBuilder, CalciteExecAggregatorFactory, CalciteExecAggregator, InternalCalciteExec. SQLPlugin now implements SearchPlugin and returns the AggregationSpec. Integration test verifies a well-formed empty result from a search request carrying `calcite_exec`.
- Files changed: `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/CombineDescriptor.java`, `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/CalciteExecAggregationBuilder.java`, `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/CalciteExecAggregatorFactory.java`, `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/CalciteExecAggregator.java`, `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/InternalCalciteExec.java`, `plugin/src/main/java/org/opensearch/sql/plugin/SQLPlugin.java`, `integ-test/src/test/java/org/opensearch/sql/calcite/remote/CalciteExecAggregationIT.java`
- **Learnings for future iterations:**
  - The `opensearch` module is the ONLY module with the full opensearch server jar; `core` has only the thin `opensearch-core`. Any class touching `InternalAggregation`, `AggregatorFactory`, `MetricsAggregator`, or `SearchContext` must live in `opensearch` (or `plugin`), never `core`. This constrains US-007's StagePlanner, which the design doc assigns to `core` — StagePlanner must therefore NOT reference any opensearch-server aggregation type.
  - `SQLPlugin` now implements `SearchPlugin`; future stories adding `getQueries()`, `getSearchExts()`, or more `AggregationSpec`s extend the existing `getAggregations()` rather than re-adding the interface.
  - Concrete OpenSearch 3.9.0-SNAPSHOT API signatures discovered: `AggregatorFactory.createInternal(SearchContext, Aggregator, CardinalityUpperBound, Map<String,Object>)`, `MetricsAggregator(name, context, parent, metadata)`, `metadata` not `metaData`, no `doEquals`/`doHashCode`, no `getMinimalSupportedVersion()` — so later stories do not have to rediscover them.
  - Writeable round-trip symmetry is the highest-risk defect class in this PoC and is invisible to single-node tests — always verify the `StreamInput` ctor reads in the same order/methods as `writeTo` writes. Note that `writeVIntArray` and `readList(StreamInput::readVInt)` ARE wire-compatible, but prefer `writeCollection(list, StreamOutput::writeVInt)` for visible symmetry.
  - `InternalCalciteExec.reduce()` currently has no rowBudget enforcement (TODO tagged US-013); CONCAT accumulates unbounded on the coordinator.
  - The design doc's worked-example JSON is the authoritative source for wire field names; it mixes snake_case (`row_budget`) at the top level with camelCase inside the combine descriptor. This is intentional — do not "normalize" it.
---

## 2026-08-17 - US-003
- Implemented the fragment codec: `RelFragmentCodec` serializes a shard-local RelNode subtree to RelJson and base64-encodes it, and deserializes the reverse by building a `RelOptCluster` (VolcanoPlanner + RexBuilder over `OpenSearchTypeFactory.TYPE_FACTORY`), registering a schema named `OpenSearch` holding the shard row-source table under the index name, and wiring the chained PPL operator table. `ExtendedRelJson` is injected into both `RelJsonWriter` and `RelJsonReader` through their Calcite 1.42 `UnaryOperator<RelJson>` constructors so PPL operators round-trip. `ShardRowSourceTable` is a `ScannableTable` stub reading rows from a `DataContext` stash slot, forward-compatible with US-005.
- Files changed: `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/RelFragmentCodec.java` (new), `opensearch/src/test/java/org/opensearch/sql/opensearch/stage/RelFragmentCodecTest.java` (new, 3 tests), `opensearch/src/main/java/org/opensearch/sql/opensearch/storage/serde/RelJsonSerializer.java` (`getPplSqlOperatorTable()` promoted private -> public static, Javadoc added, no behaviour change), `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - The repo already had RexNode-level JSON serde (`RelJsonSerializer` + `ExtendedRelJson` in `org.opensearch.sql.opensearch.storage.serde`) but NO full-RelNode-tree serde, and no prior use of `RelJsonWriter`/`RelJsonReader` anywhere. The tree walk is new as of this story; the Rex-level machinery was reusable as-is.
  - Calcite 1.42.0's `RelJsonWriter`/`RelJsonReader` both accept a `UnaryOperator<RelJson>` customizer. Always check the actual constructor set on the classpath before coding against Calcite — the overload that makes custom-operator round-trip possible is not the obvious one.
  - `ExtendedRelJson.create()` + `withOperatorTable()` NPEs unless `withInputTranslator()` is chained first (the 3-arg ctor `requireNonNull`s the translator). The translator is dead on the read path because `RelJsonReader` resolves input refs itself, so make it throw rather than fabricate refs.
  - `ExtendedRelJson::translateInput` is package-private in `...storage.serde`, so classes in `...stage` cannot reference it.
  - BIGGEST GOTCHA, cost a full extra fix round: the `fields` wire list carries OPENSEARCH type names (`long`, `keyword`), not `SqlTypeName` enum names. `SqlTypeName.valueOf()` on them always throws. The first implementation used `valueOf` AND wrote tests feeding `"BIGINT"`/`"VARCHAR"`, so the suite was green while every real caller would have failed. Always source unit-test fixtures from the design doc's authoritative wire example, not from whatever makes the code compile.
  - Prefer the existing wire type (`CalciteExecAggregationBuilder.FieldDescriptor`) over parallel name/type lists in any new API — the caller in a later story already holds those objects, and parallel lists can desync in length or order.
  - Review process note: `reviewer` returned APPROVE and correctly caught the dead ANY-typed translator, the parallel-list API shape, and the FQN-vs-import style break, but MISSED the `SqlTypeName.valueOf` wire-type defect because it trusted the passing tests. Orchestrator spot-checks against the design doc caught it. Green tests are not evidence when the tests and the code share the same wrong assumption.
  - Handoff research notes from this round: `.sisyphus/handoff/explorer-reljson-infra.md`
---

## 2026-08-18 - US-004
- Materialized shard rows for the fields a fragment references. New `ShardRowReader` builds one per-type `FieldReader` closure per requested field per Lucene leaf: keyword via `SortedSetDocValues`, long/integer/date/double/float/boolean via `SortedNumericDocValues` (sortable-decoded for the floating types), and anything without doc_values (notably bare `text`) via `_source`. `CalciteExecAggregator.getLeafCollector()` constructs the readers per leaf and `collect(doc)` appends exactly one `Object[]` row to a buffer; `InternalCalciteExec` now reports `rowsCollected` and `rowsEmitted`.
- Files changed: `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/ShardRowReader.java` (new, 249 lines), `opensearch/src/test/java/org/opensearch/sql/opensearch/stage/ShardRowReaderTest.java` (new, 18 tests), `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/CalciteExecAggregator.java`, `integ-test/src/test/java/org/opensearch/sql/calcite/remote/CalciteExecAggregationIT.java` (new `testCalciteExecAggregationKeywordLongAndTextField` covering `city` keyword, `balance` long, and `address` — a bare `text` field with no `keyword` subfield, which is what proves the `_source` path works under `size: 0`), `prd.json`, `progress.txt`
- Verification: `./gradlew build -x :integ-test:integTest -x :doctest:bootstrap -x :doctest:doctest` SUCCESS (1m52s), `./gradlew test` SUCCESS (18s), `./gradlew spotlessCheck` SUCCESS, `./gradlew :integ-test:integTest --tests "org.opensearch.sql.calcite.remote.CalciteExecAggregationIT"` SUCCESS (2 tests, 0 failures). `reviewer` verdict APPROVE with no CRITICAL findings.
- **Learnings for future iterations:**
  - THIS ROUND STARTED WITH UNCOMMITTED WIP ON DISK for US-004 (an earlier round evidently died after writing files but before committing). The audit found it already complete and correct, so this round changed zero lines of production code. ALWAYS `git status` before implementing — a story marked `passes: false` may already be built.
  - Numeric doc_values are sortable-encoded. `double` needs `NumericUtils.sortableLongToDouble` and `float` needs `sortableIntToFloat`; reading the raw long yields garbage that no test asserting only shape would catch.
  - `advanceExact()` is stateful per reader instance and must move forward. Build `FieldReader`s per leaf inside `getLeafCollector()`; a reader cached across leaves will silently skip docs rather than fail loudly.
  - `ShardRowReader.create()` deliberately does NOT position the `SourceLookup` — the caller owns `setSegmentAndDocument(ctx, doc)`. If a future story adds a second call site, that positioning must be repeated or `_source` fields come back null.
  - `text` fields have no doc_values at all, so the `_source` fallback is not an edge case — it is the ONLY path for them. Test indices in this repo expose `address` as a bare `text` field, which makes it the canonical fixture for exercising that path.
  - `rowsEmitted` is currently always equal to `rowsCollected`. US-009 is the story that makes them diverge; do not "simplify" the pair away in the meantime.
  - `reviewer` raised one deferred SUGGESTION (add an interim `rowsCollected >= rowBudget` fail-fast guard, since the buffer grows unbounded until US-009). Deliberately NOT taken this round to keep the story scoped — US-009 owns budget enforcement including the shard-global counter plus `getLeafCollector()` guard that `CollectionTerminatedException` requires.
  - Handoff notes from this round: `.sisyphus/handoff/general-us004-report.md`, `.sisyphus/handoff/us004-diff.patch`
---

## 2026-08-18 - US-005
- Executed the shard fragment inside the aggregator. `CalciteExecAggregator.buildAggregation()` now deserializes the base64 RelJson fragment, runs a HEP pass then a Volcano pass to reach `EnumerableConvention`, Janino-compiles the result with `EnumerableInterpretable.toBindable`, binds it to a custom `DataContext` that exposes both the root schema and the buffered rows under `RelFragmentCodec.SHARD_ROWS_STASH_KEY`, drains the `Enumerable`, and returns `InternalCalciteExec` carrying the FRAGMENT OUTPUT rows. The whole block runs inside `CalciteClassLoaderHelper.withCalciteClassLoader()`. `rowsCollected` (buffered input) and `rowsEmitted` (fragment output) diverge for the first time.
- Files changed: `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/CalciteExecAggregator.java` (row buffer `List<List<Object>>` -> `List<Object[]>`, new package-private static `executeFragment`), `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/RelFragmentCodec.java` (`SCHEMA_NAME`/`ShardRowSourceTable` promoted to public, `osTypeToSqlType()` extracted, planner now registers `ConventionTraitDef` + `RelCollationTraitDef`), `opensearch/src/test/java/org/opensearch/sql/opensearch/stage/FragmentExecutionTest.java` (new, 2 tests), `integ-test/src/test/java/org/opensearch/sql/calcite/remote/CalciteExecAggregationIT.java` (2 new ITs plus a `buildIdentityPlan()` helper; the 2 pre-existing ITs upgraded to ship real fragments), `prd.json`, `progress.txt`
- Commits: `354d17c91` (feat) and `7586df803` (refactor: remove the placeholder-plan bypass), both pushed to `poc/staged-calcite-exec`.
- Verification, re-run independently by the orchestrator rather than taken on the implementer's word: `./gradlew build -x :integ-test:integTest -x :doctest:bootstrap -x :doctest:doctest` SUCCESS (1m49s), `./gradlew test` SUCCESS, `./gradlew spotlessCheck` SUCCESS, `./gradlew :integ-test:integTest --tests "org.opensearch.sql.calcite.remote.CalciteExecAggregationIT"` SUCCESS with XML `tests="4" skipped="0" failures="0" errors="0"`; FragmentExecutionTest 2/0; RelFragmentCodecTest 3/0. `reviewer` verdict APPROVE, no CRITICAL findings.
- **Learnings for future iterations:**
  - THE BIG ONE, and the reviewer missed it: the first implementation kept the US-002/US-004 ITs green by hardcoding a check for their placeholder plan `"dHJpdmlhbA=="` (base64 `"trivial"`) and BYPASSING fragment execution for it. Tests passed, build passed, review said APPROVE — and production had a mode where the plan is silently ignored on the data-node path. When a story makes an earlier story's fixture invalid, UPGRADE THE FIXTURE; never add a production branch to accommodate it. Watch for this shape specifically: sentinel values, null-plan guards, and try/catch blocks that fall back to returning raw rows.
  - `reviewer` APPROVE is not sufficient evidence on its own. Across US-003 and US-005 it has now missed one real defect each round (the `SqlTypeName.valueOf` wire-type bug, and this bypass) while catching only style and future-proofing items. Always spot-check the implementer's own deviation list against the design invariants — the deviations are where the defects live.
  - Two optimizer passes are needed to get from logical RelJson to a Janino `Bindable`; one Volcano pass is not enough. `EnumerableProject.implement()` throws under `Prefer.ARRAY`, and `RexOver` inside a `LogicalProject` never reaches `EnumerableWindow` without `ProjectToWindowRule`. Both failures are about plan shape, not about the rows, so a test over an unwindowed single-column plan would never expose them.
  - `DataContexts.of(Map)` looks like the obvious stash mechanism and is a dead end: generated `EnumerableTableScan` code navigates `root.getRootSchema().getSubSchema(...)` at runtime, and `DataContexts.of` has a null rootSchema. The custom `DataContext` must serve schema navigation AND the stash.
  - `Prefer.ARRAY` yields a bare scalar for a single-field output row type. Any future drain/reduce loop over fragment output needs that guard.
  - Deliberately deferred: the custom `DataContext` returns null for `DataContext.Variable.UTC_TIMESTAMP`, so a fragment with `NOW()`/`CURRENT_TIMESTAMP` NPEs in `UserDefinedFunctionUtils.restoreFunctionProperties:168`. Not patched because the correct unit (millis vs nanos) was not verified against Calcite 1.42 and a plausible-but-wrong timestamp is worse than a null pointer. Verify against the Calcite source before fixing.
  - `getTypeFactory()` should return the existing `OpenSearchTypeFactory.TYPE_FACTORY` static — it already extends `JavaTypeFactoryImpl`, so there is no reason to allocate a second type factory anywhere in this package.
  - Handoff notes from this round: `.sisyphus/handoff/general-us005-report.md`, `.sisyphus/handoff/us005-diff.patch`
---

## 2026-08-18 - US-006
- Restricted `PredicateAnalyzer` to index-accelerable predicates only, deleting the script-query emission path entirely. The predicate-path `catch`-and-wrap in `analyzeExpression` and the `ScriptQueryExpression` branch in `Visitor.tryAnalyzeOperand` are removed; `AggregateFilterAnalyzer` switched to the no-script entry point. `LIKE` is no longer pushable (previously emitted a `wildcard` query not on the design's closed list). `guardRangeFieldType()` widens range pushdown to all scalar indexed types (not just numeric/date) to avoid breaking nested-field filtered aggregations that have no Calcite fallback. 19 explain goldens re-baselined (purely lost pushdown, no semantic changes); 6 unit tests renamed/re-asserted to the new behaviour; `CalciteLikeQueryIT.test_convert_field_text_to_keyword` overridden to verify no `wildcard` clause in explain.
- Files changed: `opensearch/src/main/java/org/opensearch/sql/opensearch/request/PredicateAnalyzer.java`, `opensearch/src/test/java/org/opensearch/sql/opensearch/request/PredicateAnalyzerTest.java`, `opensearch/src/test/java/org/opensearch/sql/opensearch/request/AggregateAnalyzerTest.java`, `integ-test/src/test/java/org/opensearch/sql/calcite/remote/CalciteLikeQueryIT.java`, 19 YAML goldens under `integ-test/src/test/resources/expectedOutput/calcite/`
- Verification: `./gradlew :opensearch:test --tests 'org.opensearch.sql.opensearch.request.*'` PASS; `./gradlew test` PASS; `./gradlew spotlessCheck` PASS; `./gradlew build -x :integ-test:integTest -x :doctest:bootstrap -x :doctest:doctest` PASS; ITs green — CalciteExplainIT 266/0/0, CalciteLikeQueryIT 11/0/0, CalcitePPLExplainIT 5/0/0, ppl ExplainIT 57/0/0, WildcardQueryIT 18/0/0, sql LikeQueryIT 11/0/0, ExistsPushdownIT 2/0/0, CalciteMixedFieldTypeIT 4/0/0. Code review verdict APPROVE, no CRITICAL findings.
- **Learnings for future iterations:**
  - The scan is a coordinator-side concept only: the shard fragment's bottom node resolves to `RelFragmentCodec`'s `ShardRowSourceTable`, so the physical scan never needs to survive a RelJson round-trip. No new RelNode class was needed; a class named `OpenSearchIndexScan` already exists in the non-Calcite V2 path and adding a second would collide. The existing `CalciteEnumerableIndexScan`/`AbstractCalciteIndexScan` plus `PushDownContext` already serves as the single physical scan operator.
  - The nested-filtered-aggregation trap: narrowing pushdown can turn a WORKING query into an HTTP 500 PLANNING_ERROR because nested-field aggregations have NO Calcite fallback — `CalciteLogicalIndexScan.pushDownAggregate` returns null, `AggregateIndexScanRule` does not call `transformTo`, and a downstream rule cannot recover. Any future pushdown narrowing MUST run `CalciteExplainIT` (all 266 tests) before being believed.
  - Removing pushdown is normally correctness-safe because the residual Filter is evaluated by Calcite on the coordinator. `FilterIndexScanRule` only calls `transformTo` on a non-null result from `PredicateAnalyzer`, so a refused push leaves the Filter intact in the plan — no silent drop, no fail.
  - Losing filter pushdown also loses LIMIT pushdown: the re-baselined goldens show `requestedTotalSize=2147483647` with an `EnumerableLimit(fetch=[10000])` above. Bounded consumption is still correct (Calcite stops reading after the limit), but this is the unbounded-fetch shape the PoC exists to eliminate. US-013 (limit pushdown) owns fixing it.
  - The first two implementation passes each shipped a defect that a green build hid: (1) an `analyzeExpressionWithScriptFallback` escape hatch that re-enabled scripts for the aggregate path, and (2) a comment specifying a pushability rule the code did not actually enforce (the guard was missing). Lesson: comments claiming a rule MUST point at the guard that enforces it, or they become a false safety signal.
  - `ScriptQueryExpression` cannot be deleted outright because the sort path (`SortAnalyzer`) and the aggregation-measure path (`AggregateAnalyzer` line ~187) still use it. Only the PREDICATE emission path is deleted. A future story may eliminate the sort/measure paths, but they are out of US-006 scope.
---

## 2026-08-18 - US-007
- Added `StagePlanner` (generic placement floor + DAG cut) and the immutable `StagePlan` result type in `org.opensearch.sql.opensearch.stage`. `split(RelNode)` computes SHARD_LOCAL/NEEDS_GATHER in one memoized bottom-up pass, cuts at the highest SHARD_LOCAL node, and returns `shardFragment` (leaf swapped for a serializable `LogicalTableScan` over `[OpenSearch, <index>]`), `CombineDescriptor.concat()`, `coordinatorTree` (cut subtree swapped for a gathered-rows scan), and `shardScan` (the original scan, for US-008). `RelFragmentCodec.ShardRowSourceTable` gained a 2-arg ctor taking a stash key so the coordinator-side table can reuse it; the 1-arg ctor still defaults to `SHARD_ROWS_STASH_KEY`. No split rules — US-012..US-015 own those.
- Files changed: `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/StagePlanner.java` (new), `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/StagePlan.java` (new), `opensearch/src/main/java/org/opensearch/sql/opensearch/stage/RelFragmentCodec.java` (2-arg `ShardRowSourceTable` ctor), `opensearch/src/test/java/org/opensearch/sql/opensearch/stage/StagePlannerTest.java` (new, 16 tests), `prd.json`, `progress.txt`
- Verification, re-run independently by the orchestrator rather than taken on the implementer's word: `./gradlew :opensearch:test --tests 'org.opensearch.sql.opensearch.stage.*' --rerun-tasks` SUCCESS with XML `StagePlannerTest tests="16" skipped="0" failures="0" errors="0"` (plus RelFragmentCodecTest 3/0, FragmentExecutionTest 2/0, ShardRowReaderTest 19/0); `./gradlew test` SUCCESS; `./gradlew spotlessCheck` SUCCESS; `./gradlew build -x :integ-test:integTest -x :doctest:bootstrap -x :doctest:doctest` SUCCESS (1m49s). No IT for this story — the acceptance criteria are unit-test-only.
- **Learnings for future iterations:**
  - THE DEFECT THIS ROUND, and `reviewer` missed it again (third round in a row): the first implementation classified ANY `Project`/`Calc` as SHARD_LOCAL when its input was, so a `LogicalProject` holding `ROW_NUMBER() OVER (...)` — exactly what PPL `dedup` and `eventstats` lower to — was shipped shard-local and CONCAT-ed, restarting window numbering on every shard. Silently wrong results, no error. The tell was in the TEST, not the code: the test named `window_project_defaults_to_needs_gather` built a `LogicalSort` instead of a window and carried a comment rationalizing the bug ("dedup lowers to ROW_NUMBER inside a Project, so it IS shard-local"). Fixed with `RexOver.containsOver` guards; the replacement tests were confirmed RED first (`expected RecordType(BIGINT account_number, INTEGER age, VARCHAR gender) but was RecordType(BIGINT account_number, BIGINT rn)`) and then GREEN.
  - GENERALIZED HEURISTIC, worth applying every round: when a test's NAME does not match what its BODY constructs, or when a test comment argues for why the current behaviour is acceptable, that comment is the defect report. Grep new test files for the operator each test claims to cover and confirm the operator is actually instantiated. `reviewer` has now missed one real defect per round (US-003 `SqlTypeName.valueOf`, US-005 sentinel-plan bypass, US-007 window placement) while catching only style and future-proofing items — treat APPROVE as necessary, never sufficient.
  - "Distribution-preserving" is a property of the EXPRESSIONS, not just the operator class. `Project`/`Calc`/`Filter` are the design doc's distribution-preserving trio, but a window function hiding inside a Project breaks that. Any future widening of the SHARD_LOCAL set must ask what can hide inside the node, not just what the node is called. Candidates to scrutinize later: correlated subqueries in a Project, and `RexSubQuery` generally.
  - The design doc's dedup worked example shows `ROW_NUMBER` INSIDE the shard fragment. That is NOT a contradiction of the RexOver guard — it is the output of split rule 3 (`RANK_LIMIT`), where the coordinator re-applies the rank limit. Reading a worked example that already has a rule applied as if it were generic-floor behaviour is an easy way to reintroduce this bug. Under the floor alone, windows gather.
  - `core` cannot see `opensearch`; the dependency is `opensearch --api--> core`, one way. The design doc's module assignments for `core` are unreliable for anything in the stage package. Check `build.gradle` before believing a module assignment.
  - `Filter` needs no RexOver guard because the Calcite 1.42.0 validator rejects a window function in a filter condition — verified, not assumed. If a later story constructs Filters programmatically rather than through the validator, revisit.
  - Gradle test caching will report `BUILD SUCCESSFUL ... up-to-date` with ZERO tests actually executed, which looks exactly like a passing run. Always pass `--rerun-tasks`, or read the test counts straight out of `opensearch/build/test-results/test/TEST-*.xml`, before believing a green result.
  - Constructing a real `CalciteLogicalIndexScan` in a unit test is cheap: `new CalciteLogicalIndexScan(cluster, relOptTable, osIndex)` with a Mockito-mocked `OpenSearchIndex` whose `getIndexName()` returns a mocked `OpenSearchRequest.IndexName`. Prefer that over mocking `AbstractCalciteIndexScan` itself, so `getRowType()`/`getCluster()` behave for real.
  - Handoff notes from this round: `.sisyphus/handoff/explorer-us007-design-spec.md`, `.sisyphus/handoff/explorer-us007-code-inventory.md`, `.sisyphus/handoff/general-us007-report.md`, `.sisyphus/handoff/us007-diff.patch`
---

## 2026-08-18 - US-008 (INCOMPLETE - left passes:false after 3 fix attempts)
- Wired end-to-end staged execution. Hook is `OpenSearchExecutionEngine.execute(RelNode, CalcitePlanContext, ResponseListener)`; gate is `StagePlan.staged() && plugins.calcite.pushdown.enabled == false`. `executeStagedPlan` builds a size-0 SearchRequest with one `calcite_exec` aggregation and `allowPartialSearchResults(false)`, issues it via `NodeClient.search`, pulls `InternalCalciteExec` from the response aggregations (tier 1 CONCAT already applied by `reduce()`), then runs `coordinatorTree` over the gathered rows through the new `CoordinatorTreeExecutor` (tier 2) and converts output rows to a `QueryResponse`. Three IT subclasses re-run the parents' own test methods under the PoC posture.
- Files changed: `opensearch/.../executor/OpenSearchExecutionEngine.java`, `opensearch/.../stage/CoordinatorTreeExecutor.java` (new), `opensearch/.../stage/ShardRowReader.java`, `opensearch/.../stage/RelFragmentCodec.java`, `opensearch/.../stage/CalciteExecAggregator.java`, `opensearch/.../stage/InternalCalciteExec.java`, `opensearch/.../storage/serde/ExtendedRelJson.java`, `core/.../data/model/ExprValueUtils.java`, tests `CoordinatorTreeExecutorTest` (new), `StagedRequestShapeTest` (new), `RelFragmentCodecTest`, `ShardRowReaderTest`, and `integ-test/.../remote/StagedCalcite{Where,Fields,Eval}CommandIT.java` (new)
- Status: criteria (a) request shape, (b) tier-1 reduce, (c) tier-2 coordinator execution, (e) no PIT/scroll/search_after, (f) build+tests are MET. Criterion (d) is NOT: `StagedCalciteWhereCommandIT` is 41 tests / 3 skipped / **4 failures**; Fields 39/0/0 and Eval 9/0/0 pass. Failures went 22 -> 7 -> 4 over three rounds. Committed as WIP, **not pushed** \u2014 pushing 4 known-failing ITs would turn CI red.
- Verification at the point of stopping: `./gradlew build -x :integ-test:integTest -x :doctest:bootstrap -x :doctest:doctest` SUCCESS, `./gradlew test` SUCCESS, `./gradlew spotlessCheck` SUCCESS, stage unit tests 46/0/0 across 6 classes, regression gate `CalciteExplainIT` 266 tests / 4 skipped / 0 failures plus the three unmodified parents green except `CalciteWhereCommandIT.testScriptFilterOnDifferentNestedHierarchyShouldThrow`, which fails on the JDBC path and predates this story.
- **Learnings for future iterations:**
  - THE SINGLE MOST USEFUL TECHNIQUE THIS ROUND, reuse it: when staged ITs fail, do not guess whether the cause is staging or the `pushdown.enabled=false` posture. Neutralize ONLY the gate (`if (false && ...)`) and re-run the same IT classes, which still set the settings themselves. That established in one run that all 89 tests pass without staging, so every failure was a staged-path defect. It also DISPROVED the previous round's gap analysis, which had confidently attributed the `query_string` failure to pushdown being disabled. A cheap controlled experiment beat two rounds of plausible reasoning.
  - The implementer reported "ITs deferred to the orchestrator" and shipped a green unit-test suite. The story's whole point (criterion (d)) was the ITs, and they were 22-red. Unit-green plus unrestricted claims of success is not evidence for an integration story \u2014 run the acceptance ITs yourself before believing any of it.
  - `reviewer` finally earned its keep: it caught BOTH criticals this round (the `shardScan()` vs `shardFragment()` row type, and the IT settings leak) after missing one real defect per round in US-003/005/007. What changed was the prompt \u2014 it was given the specific historical defect shapes by name (sentinel bypass, silent fallback, test-name/body mismatch) and told to hunt each one and report the checklist. Do that every round.
  - INTEGRATION TESTS SHARE ONE CLUSTER. `integTest` runs every IT class against a single cluster, so any transient cluster setting an IT class sets in `init()` leaks into every class that runs afterwards, in JUnit's non-deterministic order. Always restore to `null` in teardown; the precedent is `integ-test/.../remote/CalcitePPLAggregationPaginatingIT.java:18-22`.
  - The gathered-rows row type is `stagePlan.shardFragment().getRowType()`, NEVER `stagePlan.shardScan().getRowType()`. They coincide only when the cut IS the bare scan, and under the generic floor a shard-local Project or Filter above the scan makes the cut higher. Using the scan's type silently reads wrong column indices \u2014 no exception, wrong data.
  - `OpenSearchClient.search(OpenSearchRequest)` returns an `OpenSearchResponse` that parses the raw `InternalAggregation` into `ExprValue` iterators, so it CANNOT return the real `InternalCalciteExec`. The staged path uses `client.getNodeClient().get().search(searchRequest).actionGet()` directly. Consequence: staged execution is NodeClient-only and throws on the `OpenSearchRestClient` path.
  - `allowPartialSearchResults` lives on `SearchRequest`, NOT on `SearchSourceBuilder`, and was never set anywhere in main before this story (invariant 7).
  - `_id` is stored BINARY in Lucene: `document.get("_id")` returns null and produces a silent zero-row result. Use `Uid.decodeId(document.getBinaryValue("_id"))`. Any future metadata field needs the same check \u2014 a stub returning a plausible-but-wrong value is worse than one that fails loudly.
  - `ExtendedRelJson`'s parent `RelJson` holds `SqlStdOperatorTable`, so the parent's package-private `toOp()` cannot resolve ILIKE, SAFE_CAST, or PPL UDFs on the `RelJsonReader` path. Reflection on the private final `operatorTable` field is genuinely the only fix: the 3-arg ctor is private, `toOp`/`toRex(RelInput,Object)` are package-private so a subclass in another package cannot override them, and `withOperatorTable()` returns a PLAIN `RelJson`, discarding every `ExtendedRelJson` customization. Verified safe: the instance is created fresh per `deserialize()` (no shared-state or thread hazard), and failure is LOUD (`CalciteException` "no operator"), not silent. The `WELL_KNOWN_OPERATORS` map is unreachable from the `RelJsonReader` path \u2014 do not rely on it.
  - OpenSearch `nested` fields cannot be filtered through `_source` extraction, and the failure is SILENT. Nested sub-documents are separate hidden Lucene documents, so extracting `address.city` from a parent's `_source` yields a flattened List of every nested value; a Calcite filter comparing that List to a scalar is always false, giving 0 rows and no error. Mapping `nested`/`object` to VARCHAR makes the fragment COMPILE while returning nothing \u2014 that accommodation is why three tests were still red. Closing this needs the aggregator to collect through OpenSearch's nested scope, which is a story of its own.
  - OPEN CORRECTNESS ISSUE, wider than the one test that catches it: the staged path returns temporal values as epoch millis where the JDBC path returns formatted strings (`"2015-01-01 00:00:00"`). Only `testFilterScriptPushDownWithPPLBuiltInFunction` catches it today, purely because the Fields and Eval IT classes happen not to project temporal columns. Do not trust any staged result containing a date until the JDBC path's formatting is applied in the staged response builder (~20 lines).
  - `ExprValueUtils.fromObjectValue(Object, ExprType)` in `core` hardcoded `(String) o` for TIMESTAMP/DATE/TIME; it now accepts `Long` via `Instant.ofEpochMilli`. This is shared `core` code \u2014 `./gradlew test` and `CalciteExplainIT` were run as the regression gate.
  - Handoff notes from this round: `.sisyphus/handoff/explorer-us008-coordinator-path.md`, `.sisyphus/handoff/explorer-us008-test-and-reduce.md`, `.sisyphus/handoff/explorer-us008-design-spec.md`, `.sisyphus/handoff/explorer-us008-apis.md`, `.sisyphus/handoff/oracle-us008-decisions.md`, `.sisyphus/handoff/general-us008-report.md`, `.sisyphus/handoff/general-us008-fix-report.md`, `.sisyphus/handoff/general-us008-baseline.md`, `.sisyphus/handoff/general-us008-fix2-report.md`
---

## 2026-08-19 - US-008 (COMPLETE — the 4 red ITs are now green)
- Closed the two defects the previous round left open, which were the whole of acceptance criterion (d). No new classes, no new settings, no wire-format change.
  - TEMPORAL: the staged row-conversion loop in `executeStagedPlan` called the single-arg `ExprValueUtils.fromObjectValue(Object)`, which dispatches on the RUNTIME CLASS, so shard rows carrying epoch-millis `Long`s produced `1420070400000` where the JDBC path produces `"2015-01-01 00:00:00"`. Now an `ExprType[]` is pre-computed ONCE from `coordinatorTree.getRowType()` outside the row loop and handed to the two-arg `fromObjectValue(Object, ExprType)`; the schema-building loop reuses the SAME array, so emitted values and declared schema cannot diverge. A null guard returning `LITERAL_NULL` was added to the two-arg overload in `core`.
  - NESTED: filters on `nested` fields returned 0 rows silently. Nested paths are now resolved on the SHARD via `CalciteExecAggregator.findNestedPath` (walks the dotted path leaf-upward against `SearchContext.getQueryShardContext().getObjectMapper(parent)`; first hit is the innermost path) and passed into `ShardRowReader`, which gained `readRows(doc)` expanding one parent document into one row per nested sub-document, with fields under the same nested path read from the SAME sub-document. Absent-from-`_source` stays a sparse parent (one row, null nested values); present-but-wrong-shape now THROWS naming the path, field, and actual runtime class.
- Files changed: `opensearch/.../executor/OpenSearchExecutionEngine.java` (+25/-12), `opensearch/.../stage/ShardRowReader.java` (+183/-12), `opensearch/.../stage/CalciteExecAggregator.java` (+68/-5), `core/.../data/model/ExprValueUtils.java` (+3), `opensearch/.../stage/ShardRowReaderTest.java` (+173, 5 new tests). Commit `66a506279`, pushed together with the previous round's unpushed WIP commit `5279ec01a`.
- Verification, every number re-run by the orchestrator and read from JUnit XML rather than taken from the implementer: `StagedCalciteWhereCommandIT` 41 tests / 3 skipped / **0 failures** (was 4), `StagedCalciteFieldsCommandIT` 39/0/0, `StagedCalciteEvalCommandIT` 9/0/0, `CalciteExecAggregationIT` 4/0/0, regression gate `CalciteExplainIT` 266/4 skipped/0, `CalciteStatsCommandIT` 63/0/0, `CalciteFieldsCommandIT` 39/0/0, `CalciteEvalCommandIT` 9/0/0. Build (`-x :integ-test:integTest -x :doctest:bootstrap -x :doctest:doctest`) SUCCESS, `./gradlew test` SUCCESS with zero failures across every module's XML, `spotlessCheck` SUCCESS. Stage unit tests 51/0/0 across 6 classes. All four previously-failing test names were confirmed PRESENT AND PASSING in the XML, not silently skipped.
- **Learnings for future iterations:**
  - Dispatching on a Java runtime class to build an `ExprValue` is wrong anywhere the value crossed a wire. The wire is lossy about intent — an epoch-millis timestamp and a plain long are the same `Long`. Always derive the target type from the PLAN (`RelDataType` -> `ExprType`) and pass it explicitly. The bug here was invisible for two rounds only because the Fields and Eval IT classes happen not to project temporal columns.
  - Deriving the response VALUES and the response SCHEMA from one shared pre-computed array is worth doing for its own sake: it makes "schema says timestamp, value is a long" structurally impossible rather than merely tested-for.
  - When a fix needs extra per-field metadata on the shard, prefer resolving it from the shard's own mapping over adding a field to the wire type. Shard-side resolution needed ~15 lines and zero round-trip risk; widening `FieldDescriptor` would have needed symmetric `writeTo`/`StreamInput`/XContent/`equals`/`hashCode` plus a round-trip test, in the defect class this codebase has repeatedly failed to see.
  - SIBLING ALIGNMENT is the whole substance of nested expansion, and it is untestable with a single sub-document — with one sub-doc, correct alignment and a buggy cross-product produce identical output. Any test for a cardinality-changing transform must use enough input that the wrong implementation produces a DIFFERENT count. The alignment test here uses 3 sub-documents (correct: 3 rows; cross-product: 9).
  - "Absent" and "malformed" must never share a branch. Both were collapsed into "return nulls", which reproduces in miniature the exact silent-zero-row bug being fixed. Absent is a legitimate sparse document and must stay quiet; malformed must be loud. The reviewer flagged this as merely BORDERLINE, and taking it anyway was cheap — when a finding is about a silent-degrade path in a change whose entire purpose is eliminating a silent degrade, treat it as CRITICAL regardless of how it is labelled.
  - Naming the project's historical defect shapes in the `reviewer` prompt worked for the second round running: APPROVE with one borderline silent-degrade finding and two NITs (dead test code, a comment saying "Don't break" directly above a `break;`), all three fixed rather than deferred. Keep supplying the hunted-each-one checklist.
  - PROVE a pre-existing failure, never assert it. `CalciteWhereCommandIT.testScriptFilterOnDifferentNestedHierarchyShouldThrow` failed after the change; `git stash push` + re-run the single test + read the XML + `git stash pop` showed it failing identically on a clean tree. That mattered here specifically because the change touched shared `core` code (`ExprValueUtils`) and the test asserts on a thrown stack trace, so "it was already failing" was a claim with a real chance of being wrong.
  - `grep -c '<failure'` across every `build/test-results/**/*.xml` is a better completion check than any Gradle exit code, and it catches the case where a task was reported up-to-date with zero tests executed.
---

## 2026-08-19 - US-009
- Enforced the shard row budget as a hard failure. `CalciteExecAggregator.collect()` now throws the new `RowBudgetExceededException` (extends `OpenSearchException`) as soon as `rowsCollected > rowBudget`, with the design doc's message shape: `<forcingOperator> requires all rows on one node; gathered 3 rows, budget 2` (grouping separators via `%,d`; subject falls back to the literal `"this query"` when no operator forced the gather). The aggregator also charges estimated buffered-row bytes to the request circuit breaker via the inherited `AggregatorBase.addRequestCircuitBreakerBytes`, accumulating and flushing every 1024 rows plus a final flush in `buildAggregation()` and immediately before the budget throw. The forcing operator is computed on the coordinator by `StagePlanner` (the immediate parent of the cut) and shipped as a new nullable `forcing_operator` wire field on the `calcite_exec` aggregation. No new setting; `rowBudget` keeps its 200000 default.
- Files changed: `opensearch/.../stage/RowBudgetExceededException.java` (new), `opensearch/.../stage/CalciteExecAggregator.java`, `CalciteExecAggregationBuilder.java`, `CalciteExecAggregatorFactory.java`, `StagePlan.java`, `StagePlanner.java`, `opensearch/.../executor/OpenSearchExecutionEngine.java`, tests `StagePlannerTest` (+5), `RowBudgetExceededExceptionTest` (new, 4), `CalciteExecAggregationBuilderWireTest` (new, 4), `CalciteExecAggregatorEstimateTest` (new, 6), `integ-test/.../remote/CalciteExecAggregationIT.java` (+2 ITs). Commit `55aa355fd`.
- Verification, every number re-run by the orchestrator and read from JUnit XML rather than taken from the implementer: stage unit tests **70/0/0** across 9 classes (StagePlannerTest 21, ShardRowReaderTest 24, CalciteExecAggregatorEstimateTest 6, RelFragmentCodecTest 4, CalciteExecAggregationBuilderWireTest 4, RowBudgetExceededExceptionTest 4, CoordinatorTreeExecutorTest 3, FragmentExecutionTest 2, StagedRequestShapeTest 2); `./gradlew build -x :integ-test:integTest -x :doctest:bootstrap -x :doctest:doctest spotlessCheck` SUCCESS with ZERO `<failure`/`<error` matches across every `build/test-results/**/*.xml` in the repo; `CalciteExecAggregationIT` **6/0/0** with `testRowBudgetBreachFailsFast` confirmed PRESENT AND PASSING in the XML (not skipped); regression gate unchanged from US-008 — `StagedCalciteWhereCommandIT` 41/3 skipped/0, `StagedCalciteFieldsCommandIT` 39/0/0, `StagedCalciteEvalCommandIT` 9/0/0. `reviewer` verdict APPROVE with one SUGGESTION and two NITs, all three taken rather than deferred.
- **Learnings for future iterations:**
  - THE DESIGN DOC LISTS TWO THROW SITES IN `collect()` AND THEY ARE NOT INTERCHANGEABLE. `CollectionTerminatedException` + a `getLeafCollector()` guard is for an EXACTLY PUSHABLE limit (rule 4, US-013). The SAFETY BUDGET breach must be a hard exception. Using `CollectionTerminatedException` for the budget would silently truncate — it is caught per-leaf by `ContextIndexSearcher#searchLeaf()` and the leaf loop simply continues — which is exactly the Design Invariant 3 violation the story exists to prevent. Reading the doc's two bullets as one mechanism is the easy mistake here.
  - A bare `OpenSearchException` subclass does NOT need to be registered in OpenSearch's exception registry for its MESSAGE to reach the REST response. Verified end-to-end: the shard-side `RowBudgetExceededException[... gathered 3 rows, budget 2]` text arrives intact at the coordinator and into the HTTP error body. The type is lost, the message is not — so assert on message text, never on the exception class name, in ITs.
  - `allow_partial_search_results=false` MUST BE ON THE REQUEST URL for a raw `_search` IT. It defaults to true, and without it a shard-level exception comes back as a `_shards.failed` entry inside an HTTP 200 with partial results, so a budget-breach test would PASS-as-in-not-throw while proving nothing. The staged path already sets it programmatically (`SearchRequest`, not `SearchSourceBuilder`), but a hand-built REST request does not inherit that.
  - `addRequestCircuitBreakerBytes(long)` is inherited protected on `AggregatorBase` (verified at `/local/home/daichen/os-ws/OpenSearch/server/src/main/java/org/opensearch/search/aggregations/AggregatorBase.java:169`); positive values go through `addEstimateBytesAndMaybeBreak`, negative through `addWithoutBreaking`. RELEASE IS AUTOMATIC — `AggregatorBase.close()` calls `addWithoutBreaking(-requestBytesUsed)` in a finally block — so do NOT hand-release, but DO confirm nothing in the subclass overrides `close()`/`doClose()` without calling super. Charge in batches (1024, mirroring `MultiBucketConsumer`'s probe cadence), never once per row, and flush the final partial batch on EVERY exit path including the throw path.
  - GENERALIZED: when the shard needs a fact only the coordinator can know (here, which operator forced the gather), the fact must be SHIPPED, and shipping means the full symmetric-Writeable checklist — `writeTo` + `StreamInput` ctor in matching order and optional-ness, XContent output, the XContent PARSER, `equals`, `hashCode`, setter/getter, plus a round-trip test asserting whole-object equality for both null and non-null. A field missing from only the parser or only `equals` is completely invisible on a single-node test cluster. Contrast with US-008's nested paths, which were resolvable from the shard's own mapping and therefore correctly did NOT widen the wire. Ask "can the shard derive this?" first; ship only when the answer is no.
  - Name the forcing operator from the plan, not from a guessed PPL command. `getRelTypeName()` is precise and traceable; reverse-mapping `LogicalProject` to `eventstats` is not soundly derivable. But a bare `LogicalProject` actively misleads, because a Project is normally shard-local — so a `[window]` qualifier is appended when it holds a `RexOver`. The qualifier predicate was EXTRACTED into a single `StagePlanner.containsWindowFunction(RelNode)` shared with the placement pass, so the classification and the error message cannot drift apart. Do that whenever a diagnostic message restates a decision made elsewhere.
  - Boundary consistency is worth stating once and testing once: the check is `rowsCollected > rowBudget` applied AFTER the row is buffered, so exactly-budget succeeds and budget+1 throws, and the reported `gathered N` is that same N. Code, message, and IT assertion all agree. `TEST_INDEX_BANK` is 7 docs on a single shard, which makes `row_budget: 2` deterministic at `gathered 3 rows` — good enough to assert the EXACT message substring rather than a loose `contains("rows")`, which is what the IT now does.
  - Handoff notes from this round: `.sisyphus/handoff/explorer-us009-spec.md`, `.sisyphus/handoff/general-us009-report.md`, `.sisyphus/handoff/us009-diff.patch`, `.sisyphus/handoff/us009-full.patch`
---

## 2026-08-19 - US-010
- Measured the truthful staged-execution baseline. Added seven new `StagedCalcite*IT` subclasses (Head, PPLSort, Top, Stats, Dedup, PPLEventstats, Explain) so that all ten IT classes named in the design doc's Phase A acceptance plan can run under the PoC posture, ran all ten, bucketed every result from JUnit XML, and committed the table into `docs/dev/poc-staged-calcite-exec-design.md` as `#### Measured baseline (US-010)` (inserted inside `### Phase A`, before `### Phase B`). No production source touched; no failing test fixed — failures ARE the deliverable of a measurement story.
- **Result: 483 tests / 85 skipped / 333 pass / 65 fail-on-assertion / 0 fail-on-ceiling.** Green classes: Fields 39/0/0, Where 41/3/0, Eval 9/0/0, Head 6/2/0, PPLSort 18/0/0, Explain 266/80/0. Red classes: Top 9 tests/9 fail, Stats 63/26, Dedup 5/4, Eventstats 27/26.
- All 65 fail-on-assertion entries are annotated SUSPECTED REAL DEFECT; ZERO are explain re-baselines. Causes, verified from the XML text: `CannotPlanException` (no ENUMERABLE conversion rules) 33 — Eventstats 26, Top 6, Dedup 1; `UnsupportedOperationException` 12 — Stats 9, Dedup 3; RelJson `cannot serialize enum value to JSON: SqlTypeName.<X>` 9 — Stats (BIGINT x7, DECIMAL x1, INTEGER x1); shard `NullPointerException` 8 — Stats 5, Top 3; `Unable to implement EnumerableCalc` 2 — Stats 2; `can not write type [class java.math.BigDecimal]` 1 — Stats 1.
- Files changed: seven new `integ-test/src/test/java/org/opensearch/sql/calcite/remote/StagedCalcite*.java`, `docs/dev/poc-staged-calcite-exec-design.md` (+39 lines, purely additive), `prd.json`, `progress.txt`. Commit `f94b7185a`, pushed to `poc/staged-calcite-exec`.
- Verification: `./gradlew build -x :integ-test:integTest -x :doctest:bootstrap -x :doctest:doctest` SUCCESS (1m50s), `./gradlew test` SUCCESS (18s), `./gradlew spotlessCheck` SUCCESS, and ZERO `<failure`/`<error` elements across every `*/build/test-results/**/TEST-*.xml` in the repo. All ten staged classes re-run by the orchestrator with `--rerun-tasks`; the seven new classes' XML archived to `.sisyphus/handoff/us010-xml/` so a later gradle run cannot erase the evidence.
- **Learnings for future iterations:**
  - GRADLE DELETES THE WHOLE `integ-test/build/test-results/integTest/` DIRECTORY AT THE START OF EVERY `integTest` INVOCATION. The implementer ran the ten classes in batches, so the final batch wiped the XML for the seven classes measured in earlier batches, and every number it reported was unverifiable at hand-off time. If a story's deliverable IS the numbers, either run every class in ONE invocation or copy the XML somewhere outside `build/` immediately after each batch. Archiving to `.sisyphus/handoff/us010-xml/` is now the pattern.
  - A SUITE THAT SELF-SKIPS UNDER THE POSTURE YOU ARE MEASURING WILL REPORT A FLATTERING GREEN. `StagedCalciteExplainIT` came back 266 tests / 0 failures, which reads as "explain is unaffected by staging" — but 76 of its 80 skips are the suite's own `assumeTrue("This test is only for when push down is enabled")`. Real staged explain coverage is 186/266. ALWAYS read the skip MESSAGES, not just the skip count, before quoting a pass rate; and distinguish inherited `@Ignore` skips (Head's 2 come from the parent `org.opensearch.sql.ppl.HeadCommandIT`, issue 703) from posture-induced assumption skips. A high skip count on a green class is a finding, not noise.
  - `0 fail-on-ceiling` IS NOT EVIDENCE THE CEILING WORKS. Nothing in the ten classes approaches the 200000-row budget, so the refusal path is exercised only by US-009's own dedicated IT. Recorded as an explicit caveat in the doc rather than left for a reader to misread as validation. Whenever a bucket comes back empty, say WHY it is empty.
  - THE ORCHESTRATOR'S OWN ARITHMETIC IS A DEFECT SITE TOO. The pass total was transcribed as 332 (from a bad mental subtraction) while the ten per-class cells sum to 333, and `483 - 85 - 65 = 333`. `reviewer` caught it by checking the totals row against the cells — the one thing it was explicitly asked to verify cell by cell. For any story whose deliverable is a table, add a machine check that every row satisfies `tests - skipped == pass + fail-on-assertion + fail-on-ceiling` and that the annotation counts sum to the failure total, rather than trusting a hand-summed footer.
  - MEASUREMENT STORIES NEED A CI DECISION, AND THE STORY TEXT WILL NOT CONTAIN IT. Committing seven new IT classes of which four are red would have turned the shared branch red and violated the "keep CI green" quality bar, while deleting them would have destroyed the reproducible harness. The resolution: commit all seven, and put a class-level `@Ignore` on exactly the four red ones whose reason states the measured failure counts, the actual exception shapes, and the story ID that unblocks it (US-012 for Aggregate partial/final and MERGE_AGG, US-014 for Sort+Fetch top-N, US-015 for the window rank-filter). US-012/014/015 each delete one annotation as part of their acceptance. This is self-documenting tech debt rather than a hidden gap, and it is the only reason `spotlessCheck`/`build`/`test` are green with 65 known failures on the branch.
  - THE DESIGN DOC'S PER-CLASS TEST COUNTS ARE STALE AND SHOULD NOT BE USED AS EXPECTATIONS. The Phase A table claims `stats` is 4 tests and `top` is 5; they are actually 63 and 9. Any future story that gates on "N tests in class X" must read N from the XML, not from the doc.
  - THE FAILURE SHAPES MAP CLEANLY ONTO THE UNIMPLEMENTED SPLIT RULES, which is the useful signal for US-012 onward: `CannotPlanException` dominates the window/aggregate classes (no enumerable implementation reachable for those plans on the staged path), and Stats additionally exposes TWO defects that are NOT about split rules at all and will not be fixed by adding one — RelJson cannot serialize a bare `SqlTypeName` enum inside an aggregate call (9 tests), and the staged response writer cannot write a `BigDecimal` (1 test). US-012 should expect to fix the codec and the writer as well as the rule.
  - Handoff notes from this round: `.sisyphus/handoff/explorer-us010-spec.md`, `.sisyphus/handoff/general-us010-baseline.md`, `.sisyphus/handoff/us010-xml/` (archived JUnit XML for the seven new classes)
---

## 2026-08-19 - US-011
- Explain now renders the stage split. `ExplainResponseNodeV2` gained `shardFragment`, `combine`, `coordinatorTree`; `OpenSearchExecutionEngine.explain` populates them via a new `populateStagedExplain` for ALL FOUR explain modes (SIMPLE, STANDARD, EXTENDED, COST). The staged gate was extracted into a single `splitIfStaged(RelNode)` now called by BOTH `execute` and `explain` on their identical `rel` parameter, so the described split and the executed split cannot drift. `shardFragment` splices `stagePlan.shardScan()` back over the fragment's serializable `LogicalTableScan` leaf (`replaceSerializableLeafWithRealScan`) so the real scan's `explainTerms` render. `CombineDescriptor.describe()` emits the design doc's one-line shape (`CONCAT`, or `MODE{k:v, ...}`). No new setting, no wire-format change, no `StagePlanner` placement change.
- Files changed: `core/.../executor/ExecutionEngine.java`, `opensearch/.../executor/OpenSearchExecutionEngine.java`, `opensearch/.../stage/CombineDescriptor.java`, new `integ-test/.../remote/CalciteStageSplitExplainIT.java` (2 tests), new golden `expectedOutput/calcite_no_pushdown/explain_staged_where_fields.yaml`, new `opensearch/src/test/.../stage/CombineDescriptorTest.java` (6 tests) and `.../executor/StagedExplainLeafSpliceTest.java` (1 test), plus 188 re-baselined goldens under `expectedOutput/calcite_no_pushdown/`. Commit `316bad37f`, pushed.
- Verification, every number re-run by the orchestrator and read from JUnit XML: **CalciteExplainIT 266/4 skipped/0 failures** (the proof non-staged explain is untouched), **StagedCalciteExplainIT 266/80 skipped/0** (was 163 failures before re-baselining), **CalciteStageSplitExplainIT 2/0/0**, StagedCalciteWhereCommandIT 41/3/0, StagedCalciteFieldsCommandIT 39/0/0, StagedCalciteEvalCommandIT 9/0/0, CalciteExecAggregationIT 6/0/0 — all seven in ONE integTest invocation with `--rerun-tasks`. `./gradlew build -x :integ-test:integTest -x :doctest:bootstrap -x :doctest:doctest` SUCCESS, `./gradlew test spotlessCheck --rerun-tasks` SUCCESS, ZERO `<failure`/`<error` across every live `*/build/test-results/**/TEST-*.xml`. `reviewer` verdict APPROVE with one SUGGESTION and one NIT, both taken.
- **Learnings for future iterations:**
  - THE HIGHEST-VALUE THING THIS ROUND WAS A MACHINE-CHECKABLE GATE ON THE GOLDEN DIFF, and it caught a real defect that every green-test signal missed. The first implementation pass re-baselined 188 goldens and reported the deletions as "trailing-newline normalization". They were not: `agg_case_cannot_push.yaml` came back with `CASE(<($10, 30), 'u30', <($10, 40), 'u40', 'u100')` where HEAD records `SEARCH($10, Sarg[[30..40]])` — a semantic plan-text change, in a file whose plan had no business changing. For ANY story that re-baselines goldens, state the permitted diff shape UP FRONT and prove it mechanically: `git diff -U0 | grep '^-' | grep -v '^---'` must be empty except for lines re-added identically, and every changed `.json` golden must parse to an object equal to HEAD's minus exactly the intended new keys. A summed +/- line count is not a gate.
  - WHEN A DIFF ANOMALY HAS NO MECHANISM, RUN THE TWO-ARM EXPERIMENT BEFORE THEORIZING. Stash everything and run the one failing test (ARM A: clean HEAD); then restore ONLY the source files, leaving goldens at HEAD, and run it again (ARM B). ARM A passed and ARM B failed with a diff containing NOTHING but the three added sections — identical `logical`/`physical` text. That single pair of runs proved the source change was innocent, proved the re-baselined content was invalid, and produced the true failure count (163, not the reported 4) in one shot. The Sarg regeneration remains UNEXPLAINED and is recorded as such; it did not reproduce, and inventing a mechanism for it would have been worse than admitting ignorance.
  - A REPORTED FAILURE COUNT FROM A RUN AGAINST SELF-MODIFIED GOLDENS IS MEANINGLESS. "4 failures, pre-existing HEP non-determinism" was really "163 failures, all of them my own new sections, of which 4 also had corrupted plan text". Any failure count must be quoted against a KNOWN golden baseline, and "non-determinism" is never acceptable as a diagnosis unless the same test is run twice on an unchanged tree and shown to produce two different outputs.
  - `PPLIntegTestCase.loadExpectedPlan:484` switches golden directory on a CLUSTER-SETTING READ (`isPushdownDisabled()` at line 398 reads the transient `plugins.calcite.pushdown.enabled`): enabled -> `expectedOutput/calcite/` (372 files), disabled -> `expectedOutput/calcite_no_pushdown/` (167 files). This is the single fact that bounds the blast radius of ANY staged-path output change, and it is why `expectedOutput/calcite/` staying empty-diff is the load-bearing proof that the production default is untouched. Any future story that changes staged output should compute its blast radius from this first.
  - NULL-OMITTING SERIALIZATION IS A FREE COMPATIBILITY MECHANISM HERE, AND IT ALREADY EXISTED: `YamlFormatter` sets `JsonInclude.Include.NON_NULL` (`core/.../utils/YamlFormatter.java:32`) and the JSON path uses Gson, which omits nulls by default and never calls `serializeNulls()`. So new `ExplainResponseNodeV2` fields left null are invisible. This is why NO gating on explain mode was needed and no new setting was added — resist the urge to add a flag when the serializer already gives you the behaviour.
  - ADDING A FIELD TO `ExplainResponseNodeV2` MEANS TOUCHING `ExplainResponse.normalizeLf` TOO (`core/.../executor/ExecutionEngine.java:144`). It rebuilds the node through the 3-arg constructor, so any field not explicitly re-set there is silently DROPPED on the normalization path. Same defect family as the Writeable round-trip checklist: one construction site that forgets a field is invisible until a specific format is requested.
  - EXPLAIN AND EXECUTE MUST SHARE THE GATE, NOT DUPLICATE IT. An explain that describes a different split than the one `execute` runs is worse than no explain at all, and two copies of `stagePlan.staged() && isPushdownDisabled(...)` would drift the first time a condition is added. Extract the predicate. Same principle as US-009's `StagePlanner.containsWindowFunction` shared between placement and the error message.
  - The fragment leaf must be SPLICED for explain because `StagePlanner.split()` deliberately swaps the real scan for a plain `LogicalTableScan` to make RelJson round-trip; rendering `shardFragment()` raw shows a leaf with no scan detail at all. Under the PoC posture the spliced scan still renders NO DSL, because pushdown-disabled means no pushdown rules are registered (`CalciteLogicalIndexScan.java:153`) and `AbstractCalciteIndexScan.explainTerms` `itemIf`-suppresses both `PushDownContext` and `sourceBuilder` when the context is empty. That is honest and was left alone; fabricating a DSL string to make the section look populated would have been the sentinel-value defect shape in a new costume.
  - A POSTURE-ASSERTING TEST PAIR MUST NOT LIVE IN A SUBCLASSED IT. Putting the "sections are absent when pushdown is enabled" test into `CalciteExplainIT` would have made `StagedCalciteExplainIT` inherit it under the opposite posture and fail. Hence a standalone `CalciteStageSplitExplainIT extends PPLIntegTestCase` holding BOTH directions, with the mandatory `@After` restoring both settings to null. Assert on the YAML KEY (`"combine:"`), not the bare word — `combine` alone would collide with plan text such as an `mvcombine` call.
  - Only `CONCAT` is reachable today, so the new golden `explain_staged_where_fields.yaml` is the canonical floor-only shape and worth reading before starting US-012/014/015: `where` + `fields` puts `LogicalFilter` + `LogicalProject` in `shardFragment`, `combine: CONCAT`, and leaves `LogicalSystemLimit` over `LogicalTableScan(table=[[OpenSearch, _gathered_rows]])` in `coordinatorTree`. Each of US-012..US-015 adds its own golden for MERGE_AGG / LIMIT / TOP_N / RANK_LIMIT, and each will re-baseline a further slice of `calcite_no_pushdown/` — apply the same additions-only gate every time.
  - Handoff notes from this round: `.sisyphus/handoff/explorer-us011-explain-path.md`, `.sisyphus/handoff/explorer-us011-design-spec.md`, `.sisyphus/handoff/explorer-us011-golden-mechanism.md`, `.sisyphus/handoff/explorer-us011-explain-modes.md`, `.sisyphus/handoff/general-us011-diagnosis.md`, `.sisyphus/handoff/general-us011-final-report.md`, `.sisyphus/handoff/us011-xml/`
---
