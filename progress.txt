# Codebase Patterns
- PPL AST uses AbstractNodeVisitor<T, C> pattern — extend with <SqlNode, Void> for the new converter
- Pipe chaining in PPL = subquery wrapping in SQL: each command wraps prior result as `(SELECT ... FROM ...) AS _tN`
- PPLToSqlTranspiler.FUNC_MAP maps ~60 PPL function names to SQL equivalents — port to SqlNodeDSL.call() invocations
- Calcite SqlNode.toSqlString(SqlDialect.DatabaseProduct.CALCITE.getDialect()) produces readable SQL for debugging
- SqlConformanceEnum.MYSQL_5 enables BOOLEAN↔NUMBER coercion via allowLenientCoercion()
- Calcite TypeCoercion physically mutates SqlNode tree by injecting CAST nodes via call.setOperand() — CASTs propagate to RelNode
- Calcite's default commonTypeForBinaryComparison already handles STRING→NUMERIC and DATE→TIMESTAMP comparisons
- StarExceptReplaceRewriter is the existing pattern for a pre-validate SqlShuttle that accesses SchemaPlus
- PPLIntegTestCase V4 routing: when ppl.engine.v4=true, transpile PPL→SQL and send to /_plugins/_sql endpoint
- Test measurement: scripts/ppl_it_rate.sh runs all Calcite*IT with -Dppl.engine.v4=true and parses JUnit XML

# Iteration Log

## [2026-03-10T21:43Z] - US-001: Snapshot string-transpiler baseline
- Captured baseline from existing test run in `build/ppl-v4-reports/`
- Copied to `docs/dev/baseline-string-transpiler.json`, `docs/dev/baseline-string-transpiler-per-class.csv`, `docs/dev/baseline-string-transpiler-failures.txt`
- Baseline: 2264 total, 1325 passed (58.5%), 795 failed, 144 skipped
- Commit: 607eab057 `feat: [US-001] Snapshot string-transpiler baseline`
- **Learnings for future iterations:**
  - The test suite requires a running OpenSearch cluster (`integ-test:integTest`)
  - 58.5% is the regression target — new converter must pass all 1325 tests that currently pass
  - Highest failure classes: CalciteArrayFunctionIT (60/60 fail), CalciteSearchCommandIT (46/52 fail), CalciteDateTimeFunctionIT (35/65 fail)
---

## [2026-03-10T21:58Z] - US-002: Create SqlNodeDSL utility class
- SqlNodeDSL.java (327 lines) already existed as untracked file — verified all acceptance criteria met
- SelectBuilder fluent API: select().from().where().groupBy().having().orderBy().limit().offset().build()
- 30+ static factory methods: star, identifier, literal, table, subquery, asList, gt/lt/gte/lte/eq/neq, plus/minus/times/divide/mod, and/or/not, call, agg, cast, as, between, in, caseWhen, window, isNull/isNotNull, desc/nullsFirst/nullsLast, like, count/countStar/sum/avg/min/max
- SqlNodeDSLTest.java with 5 tests: selectStarFromTable, nestedSubquery, orderByWithLimit, aggregationWithGroupBy, expressionHelpers
- Build: `:api:compileJava` and `:api:compileTestJava` pass
- Tests: `SqlNodeDSLTest` all 5 pass
- Files: api/src/main/java/org/opensearch/sql/calcite/utils/SqlNodeDSL.java, api/src/test/java/org/opensearch/sql/calcite/utils/SqlNodeDSLTest.java
- Commit: 5d8706c5c `feat: [US-002] Create SqlNodeDSL utility class`
- **Learnings for future iterations:**
  - SqlNodeDSL uses SqlParserPos.ZERO for all positions — no source location tracking needed
  - call() and agg() both use SqlUnresolvedFunction with USER_DEFINED_FUNCTION category — Calcite validator resolves them against the operator table
  - SelectBuilder.build() wraps in SqlOrderBy only when orderBy/offset/fetch are set, otherwise returns plain SqlSelect
  - CalciteSqlDialect.DEFAULT quotes identifiers with double-quotes
---

## [2026-03-10T22:06Z] - US-003: Create base PPLToSqlNodeConverter with 5 core commands
- Created PPLToSqlNodeConverter.java (253 lines) extending AbstractNodeVisitor<SqlNode, Void>
- Architecture: flatten AST chain → iterate → visitor dispatch (no if/else-if chain)
- 5 core commands: visitRelation (SELECT * FROM table), visitFilter (wrap + WHERE), visitProject (SELECT cols), visitSort (ORDER BY), visitHead (LIMIT/OFFSET)
- Pipe chaining: each command wraps prior result as subquery via SqlNodeDSL.subquery()
- Minimal expression visitors: visitCompare, visitAnd, visitOr, visitNot, visitQualifiedName, visitField, visitLiteral, visitAllFields
- Static parse() helper reuses PPLSyntaxParser + AstBuilder + AstStatementBuilder (same as PPLToSqlTranspiler)
- PPLToSqlNodeConverterTest.java with 6 tests: testSource, testWhereFilter, testFieldsProject, testSortOrderBy, testHeadLimit, testFullPipeline
- Build: `:api:compileJava` and `:api:compileTestJava` pass
- Tests: all 6 pass, full `:api:test` suite passes
- Files: api/src/main/java/org/opensearch/sql/api/PPLToSqlNodeConverter.java, api/src/test/java/org/opensearch/sql/api/PPLToSqlNodeConverterTest.java
- Commit: d89fb0596 `feat: [US-003] Create base PPLToSqlNodeConverter with 5 core commands`
- **Learnings for future iterations:**
  - SqlNodeDSL.SelectBuilder is the fluent builder type (not SqlNode.SelectBuilder)
  - Calcite uses ANSI SQL syntax: OFFSET n ROWS FETCH NEXT n ROWS ONLY (not MySQL LIMIT/OFFSET)
  - The `pipe` field tracks the current SqlNode state across visitor calls — each visitXxx wraps it as subquery
  - AllFields passthrough in visitProject avoids unnecessary subquery wrapping
  - Sort field args use "asc" (boolean) and "nullFirst" (boolean) argument names
  - Head has getSize() for LIMIT and getFrom() for OFFSET
---

## [2026-03-11T00:10Z] - US-004: Port expression visitors and FUNC_MAP
- Added 16 new expression visitor methods to PPLToSqlNodeConverter.java
- Enhanced visitLiteral: DATE/TIME/TIMESTAMP → CAST('value' AS TYPE), DOUBLE/FLOAT/LONG → CAST(value AS TYPE)
- Enhanced visitCompare: added REGEXP → REGEXP_CONTAINS
- New visitors: visitAlias, visitEqualTo, visitXor, visitFunction, visitAggregateFunction, visitIn, visitBetween, visitCase, visitCast, visitSpan, visitInterval, visitWindowFunction, visitLet, visitInSubquery, visitScalarSubquery, visitExistsSubquery
- visitFunction handles: arithmetic ops (+,-,*,/,%), if→CASE WHEN, ifnull→COALESCE, isnull→IS NULL, log→LN, log2→LN/LN(2), like/not like, FUNC_MAP fallback with SQL_DATETIME_KEYWORDS
- visitAggregateFunction handles: distinct_count/dc→COUNT(DISTINCT), count(*)→COUNT(*), DISTINCT support
- Added notLike, exists, inSub helpers to SqlNodeDSL.java
- Added convertSubPlan() helper for subquery expression visitors
- 17 unit tests all pass, full :api:test suite passes
- Files: PPLToSqlNodeConverter.java (640 lines), SqlNodeDSL.java (+26 lines), PPLToSqlNodeConverterTest.java (+72 lines)
- Commit: 9e8d78e36 `feat: [US-004] Port expression visitors and FUNC_MAP`
- **Learnings for future iterations:**
  - PPL case() syntax is different from SQL CASE — conditions are positional pairs, not SQL-style WHEN/THEN
  - eval command (US-006) is needed to test expression visitors in computed column context
  - WindowFunction.getSortList() returns List<Pair<SortOption, UnresolvedExpression>>, not List<UnresolvedExpression>
  - SqlSelectKeyword.DISTINCT + field as SqlBasicCall args to SqlStdOperatorTable.COUNT creates COUNT(DISTINCT field)
  - SQL_DATETIME_KEYWORDS (CURRENT_TIMESTAMP etc.) need special handling — call with no args, not as identifier
---

## [2026-03-11T00:20Z] - US-005: Add stats (aggregation) command
- Added visitAggregation method (50 lines) to PPLToSqlNodeConverter.java
- SELECT items: aggregates first, then span (if any), then group-by fields (matches PPL convention)
- GROUP BY: span delegated expression + group-by field delegated expressions (unwrapped from Alias)
- bucket_nullable=false: adds WHERE IS NOT NULL filter on all group-by columns
- 4 unit tests: testStatsCount, testStatsGroupBy, testStatsMultipleAggs, testStatsWithSpan
- Build: `:api:compileJava` and `:api:compileTestJava` pass
- Tests: all 21 tests pass (17 existing + 4 new)
- Files: PPLToSqlNodeConverter.java (+51 lines), PPLToSqlNodeConverterTest.java (+32 lines)
- Commit: eda6947f6 `feat: [US-005] Add stats (aggregation) command`
- **Learnings for future iterations:**
  - Aggregation node stores span separately from groupExprList — must handle both
  - For GROUP BY, unwrap Alias to get the raw expression (delegated), but keep full Alias for SELECT
  - bucket_nullable default is true; only filter nulls when explicitly set to false
  - The visitAggregateFunction and visitSpan expression visitors (from US-004) handle all the expression-level work; visitAggregation just orchestrates the SELECT/GROUP BY structure
---

## [2026-03-11T00:28Z] - US-006: Add eval command
- Added visitEval method (26 lines) to PPLToSqlNodeConverter.java
- Architecture: wrap current pipe as subquery, SELECT *, expr1 AS alias1, expr2 AS alias2
- Forward-reference inlining: SqlShuttle replaces SqlIdentifier references to earlier eval aliases with their SqlNode expressions
- Self-referencing eval (column override): emits SELECT *, expr AS col — duplicate column handled downstream
- 3 unit tests: testEvalSimple, testEvalForwardReference, testEvalSelfReference — all pass
- Build: `:api:compileJava` and `:api:compileTestJava` pass
- Tests: full `:api:test` suite passes (24 tests total)
- Files: PPLToSqlNodeConverter.java (+27 lines), PPLToSqlNodeConverterTest.java (+24 lines)
- Commit: 800e583b0 `feat: [US-006] Add eval command`
- **Learnings for future iterations:**
  - SqlShuttle is the idiomatic way to transform SqlNode trees — visit(SqlIdentifier) for identifier substitution
  - Forward-reference inlining at the SqlNode level is cleaner than the string-based regex approach in PPLToSqlTranspiler
  - Eval's self-referencing case (e.g. eval a = a + 1) doesn't need special temp-alias handling at the SqlNode level — the duplicate column in SELECT *, expr AS a is resolved by Calcite validation or the next subquery wrap
  - LinkedHashMap preserves insertion order for eval aliases, which matters for forward references
---

## [2026-03-11T00:38Z] - US-007: Add dedup command
- Added visitDedupe method (77 lines) to PPLToSqlNodeConverter.java
- Standard dedup (consecutive=false, keepEmpty=false): filter nulls → ROW_NUMBER() OVER (PARTITION BY fields ORDER BY fields) → filter _dedup_rn <= N → strip helper column
- keepEmpty=true: skip null filter, add OR (field IS NULL) to row number condition
- Consecutive dedup (gaps-and-islands): ROW_NUMBER() global + per-group → island detection via (global_rn - group_rn) → ROW_NUMBER() per island → filter
- 3 unit tests: testDedupSimple, testDedupKeepEmpty, testDedupConsecutive — all pass
- Build: `:api:compileJava` and `:api:compileTestJava` pass
- Tests: full `:api:test` suite passes (27 tests total)
- Files: PPLToSqlNodeConverter.java (+77 lines), PPLToSqlNodeConverterTest.java (+25 lines)
- Commit: 2c94fb4bb `feat: [US-007] Add dedup command`
- **Learnings for future iterations:**
  - Dedupe AST node options are positional: [0]=allowedDuplication(Integer), [1]=keepEmpty(Boolean), [2]=consecutive(Boolean) — accessed via `((Literal) node.getOptions().get(N).getValue()).getValue()`
  - Standard dedup needs 4 subquery wraps: null filter → ROW_NUMBER → row filter → strip helper column
  - Consecutive dedup uses gaps-and-islands: global ROW_NUMBER + per-group ROW_NUMBER → island = (global - group) → per-island ROW_NUMBER
  - The final `SELECT * FROM (...)` wrap after filtering is needed to strip the _dedup_rn helper column from the output
  - CAST("_id" AS INTEGER) is used for ordering in consecutive dedup (same as string transpiler)
---

## [2026-03-11T00:46Z] - US-008: Add join and lookup commands
- Added visitJoin method to PPLToSqlNodeConverter.java handling all 7 join types
- INNER/LEFT/RIGHT/FULL/CROSS: mapped to Calcite JoinType, constructed via SqlNodeDSL.join() → SqlJoin node
- CROSS+condition → INNER JOIN, INNER+no_condition → CROSS JOIN (same semantics as string transpiler)
- SEMI/ANTI: rewritten to WHERE EXISTS/NOT EXISTS (SELECT 1 FROM right WHERE condition)
- Added visitLookup method: LEFT JOIN with _l/_r aliases, ON clause from mappingAliasMap
- Lookup REPLACE: SELECT _l.*, _r.lookupField AS targetField
- Lookup APPEND: SELECT _l.*, COALESCE(_l.targetField, _r.lookupField) AS targetField
- Added join() helper to SqlNodeDSL.java using Calcite SqlJoin constructor
- Added resolveJoinRight() helper: handles SubqueryAlias, Relation, and subquery pipeline right sides
- Added extractRelation() helper: unwraps Project(AllFields) → Relation
- Added buildFieldListCondition() helper: builds ON clause from field list
- 6 unit tests: testJoinInner, testJoinLeft, testJoinSemi, testJoinAnti, testLookup, testLookupWithOutput
- Build: `:api:compileJava` and `:api:compileTestJava` pass
- Tests: full `:api:test` suite passes (33 tests total)
- Files: PPLToSqlNodeConverter.java (+138 lines), SqlNodeDSL.java (+14 lines), PPLToSqlNodeConverterTest.java (+42 lines)
- Commit: 7d7439d7f `feat: [US-008] Add join and lookup commands`
- **Learnings for future iterations:**
  - Calcite SqlJoin constructor: new SqlJoin(pos, left, natural, joinType.symbol(pos), right, conditionType.symbol(pos), condition)
  - JoinType and JoinConditionType are Calcite enums (org.apache.calcite.sql.JoinType, not PPL's Join.JoinType)
  - SEMI/ANTI joins have no direct SQL syntax in Calcite — must rewrite to EXISTS/NOT EXISTS
  - For qualified star (_l.*), use new SqlIdentifier(Arrays.asList(alias, ""), POS) — empty string as last part creates star
  - Join's left side comes from pipe (already visited via flatten pattern), right side from node.getRight()
  - Lookup always uses LEFT JOIN with fixed aliases _l and _r
  - extractRelation() handles the common case where right side is Project(AllFields) wrapping a Relation
---

## [2026-03-11T01:30Z] - US-009: Add subquery, window, and remaining core commands
- Added 10 visit methods to PPLToSqlNodeConverter.java (+562 lines)
- visitRareTopN: GROUP BY + COUNT(*) + ROW_NUMBER + filter + project
- visitTrendline: SMA (AVG via window) / WMA (NTH_VALUE weighted sum) with ROWS frame
- visitWindow (eventstats): SELECT *, agg OVER (PARTITION BY ... RANGE UNBOUNDED)
- visitStreamWindow (streamstats): running window with WindowFrame-based frame spec
- visitAppend: UNION ALL via new SqlNodeDSL.unionAll() helper
- visitBin: SpanBin only — numeric (FLOOR), time-based (epoch bucketing), log-based spans
- visitReplace: REPLACE / REGEXP_REPLACE with WildcardUtils support
- visitRename: SELECT *, old AS new (non-wildcard, chained rename support)
- visitFillNull: COALESCE(field, value) per field pair
- visitParse: REGEXP_EXTRACT per named capture group (REGEX method only)
- Added windowWithFrame() and unionAll() to SqlNodeDSL.java (+18 lines)
- Added buildWindowAggSql, windowBoundToSqlNode, transpileSpanToSqlNode, getFieldName helpers
- 8 new unit tests: testRareTopN, testRare, testWindow, testAppend, testRename, testFillNull, testParse, testReplace
- Build: passes, Tests: 108 total all pass
- Files: PPLToSqlNodeConverter.java, SqlNodeDSL.java, PPLToSqlNodeConverterTest.java
- Commit: 98b924310 `feat: [US-009] Add subquery, window, and remaining core commands`
- **Learnings for future iterations:**
  - WindowFrame AST uses WindowBound subclasses (CurrentRow, Unbounded, OffSet) — map to SqlWindow.createXxx() factory methods
  - SqlWindow.create() takes isRows boolean + lower/upper SqlNode bounds for frame specification
  - eventstats uses RANGE BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING (full partition)
  - streamstats uses WindowFunction.getWindowFrame() for per-function frame specs
  - buildWindowAggSql needed because PPL parser wraps eventstats aggregates as Function not AggregateFunction
  - Bin only supports SpanBin in V4 — other bin types (MinSpanBin, CountBin, RangeBin, DefaultBin) throw UnsupportedOperationException
  - Parse only supports REGEX method — GROK and PATTERNS throw UnsupportedOperationException
  - fillnull replacementForAll (all-fields mode) deferred to DynamicPPLToSqlNodeConverter (needs schema)
  - PPL fillnull syntax is `fillnull value=X field1, field2` not `fillnull with X field1, field2`
---

## [2026-03-11T01:38Z] - US-010: Create PPLTypeCoercion with MYSQL_5 conformance
- Created PPLTypeCoercion.java (48 lines) in core/src/main/java/org/opensearch/sql/calcite/type/
- Extends TypeCoercionImpl, overrides commonTypeForBinaryComparison() to add DATE + TIME → TIMESTAMP merge rule
- Registered via TypeCoercionFactory in UnifiedQueryContext.buildFrameworkConfig(): .sqlValidatorConfig(SqlValidator.Config.DEFAULT.withTypeCoercionFactory(PPLTypeCoercion::new))
- PPLTypeCoercionTest.java with 2 tests validating coercion through full Calcite validation pipeline
- Build: `:api:compileJava` and `:api:compileTestJava` pass
- Tests: all pass (PPLTypeCoercionTest + full :api:test suite)
- Files: core/src/main/java/org/opensearch/sql/calcite/type/PPLTypeCoercion.java (new), api/src/main/java/org/opensearch/sql/api/UnifiedQueryContext.java (modified +4 lines), api/src/test/java/org/opensearch/sql/api/PPLTypeCoercionTest.java (new)
- Commit: 4e2ad0cf5 `feat: [US-010] Create PPLTypeCoercion with MYSQL_5 conformance`
- **Learnings for future iterations:**
  - Override commonTypeForBinaryComparison() instead of binaryComparisonCoercion() — cleaner, lets parent handle the coercion mechanics (coerceOperandType/CAST injection)
  - Calcite's built-in commonTypeForBinaryComparison already handles: STRING→NUMERIC, DATE→TIMESTAMP widening, CHARACTER→DATETIME — only DATE+TIME merge is missing
  - PPLTypeCoercion::new works as method reference for TypeCoercionFactory since constructor signature matches (RelDataTypeFactory, SqlValidator)
  - builtinFunctionCoercion override not needed — Calcite's default implicitCast() handles STRING→NUMERIC and STRING→DATETIME for function args
  - MYSQL_5 conformance for BOOLEAN↔NUMBER coercion is a per-query-path concern, not global — will be set when PPL V4 path is wired (US-012)
---

## [2026-03-11T01:54Z] - US-011: Create DynamicPPLToSqlNodeConverter with schema-dependent commands
- Created DynamicPPLToSqlNodeConverter.java (207 lines) extending PPLToSqlNodeConverter
- Constructor takes SchemaPlus defaultSchema, creates JavaTypeFactoryImpl for type resolution
- resolveColumns(tableName): case-insensitive table lookup → getRowType → getFieldList → getName
- visitRelation override: captures tableName for later column resolution by other visitors
- visitProject override: handles fields - (exclude) by resolving all columns from schema, filtering out excluded ones
- visitRename override: handles wildcard rename via WildcardRenameUtils.isWildcardPattern/matchFieldNames/applyWildcardTransformation
- visitFillNull override: handles all-fields mode (replacementForAll.isPresent()) by applying COALESCE to every column from schema
- visitEval override: handles column override by detecting when eval alias matches existing column, emitting explicit column list instead of SELECT *
- convertSubPlan override: returns DynamicPPLToSqlNodeConverter(defaultSchema) so subqueries also get schema access
- DynamicPPLToSqlNodeConverterTest.java with 3 tests using in-memory SchemaPlus (table "t" with columns a:INTEGER, b:VARCHAR, c:DOUBLE)
- Build: `:api:compileJava` and `:api:compileTestJava` pass
- Tests: all 3 new tests pass (testFieldsExclude, testFillNullAllFields, testEvalColumnOverride), full :api:test suite passes
- Files: api/src/main/java/org/opensearch/sql/api/DynamicPPLToSqlNodeConverter.java (new), api/src/test/java/org/opensearch/sql/api/DynamicPPLToSqlNodeConverterTest.java (new)
- Commit: c50f243a7 `feat: [US-011] Create DynamicPPLToSqlNodeConverter with schema-dependent commands`
- **Learnings for future iterations:**
  - SchemaPlus column resolution pattern: defaultSchema.getTable(name) → Table → getRowType(typeFactory) → getFieldList() → getName()
  - Case-insensitive table lookup: iterate getTableNames() if exact match fails (same as StarExceptReplaceRewriter)
  - getFieldName is private in parent class — use local fieldName() helper instead
  - Eval column override: when eval alias matches existing column, emit explicit column list with override instead of SELECT *, expr AS col (avoids duplicate column ambiguity)
  - WildcardRenameUtils (core module) is accessible from api module — matchFieldNames returns List<String> of matching field names
  - For non-wildcard renames and non-exclude projects, delegate to super to avoid code duplication
  - convertSubPlan override is critical — without it, subqueries in append/join/subquery expressions would use base PPLToSqlNodeConverter without schema access
---

## [2026-03-11T02:05Z] - US-012: Wire converter into PPLIntegTestCase V4 routing
- Replaced PPLToSqlTranspiler.transpile() with DynamicPPLToSqlNodeConverter pipeline in PPLIntegTestCase V4 routing
- Added transpileV4(String ppl) method: PPLToSqlNodeConverter.parse(ppl) → DynamicPPLToSqlNodeConverter.convert(plan) → sqlNode.toSqlString(CalciteSqlDialect.DEFAULT).getSql()
- Added lazy SchemaPlus initialization via getV4Schema(): AbstractSchema backed by OpenSearchIndex tables (same pattern as UnifiedQueryOpenSearchIT.createOpenSearchSchema())
- Added V4_SETTINGS: minimal Settings implementation returning null for all keys (sufficient for OpenSearchIndex schema resolution)
- Replaced two PPLToSqlTranspiler.transpile(query) calls with transpileV4(query) in executeQuery() and executeQueryToString()
- Build: :api:compileJava, :api:compileTestJava, :integ-test:compileTestJava all pass
- Tests: :api:test all 108 tests pass
- Files changed: integ-test/src/test/java/org/opensearch/sql/ppl/PPLIntegTestCase.java (+66 lines, -2 lines)
- Commit: 2785c09b0 `feat: [US-012] Wire converter into PPLIntegTestCase V4 routing`
- **Learnings for future iterations:**
  - SchemaPlus for V4 test path follows same pattern as UnifiedQueryOpenSearchIT: AbstractSchema with lazy-loading HashMap that creates OpenSearchIndex on first access
  - OpenSearchIndex constructor needs (OpenSearchClient, Settings, String indexName) — Settings can be minimal (return null for all keys) since it's only stored, not actively used during schema resolution
  - The v4Schema field is instance-level (not static) because client() is instance-specific in OpenSearch REST test framework
  - SchemaPlus is wrapped: createRootSchema → add("default", osSchema) → getSubSchema("default") so table lookups work directly
  - End-to-end verification requires a running OpenSearch cluster (integ-test:integTest) — unit build verification is sufficient for this story
  - The EXPLAIN query handling in executeQueryToString() is unchanged — it still routes through the PPL explain endpoint
---
