# Codebase Patterns
(Patterns discovered during implementation will be consolidated here)

# Iteration Log

<!-- Round summaries and human intervention notes will be appended here automatically -->

## Codebase Patterns
- SimpleTable is now a public top-level class at `api/src/main/java/org/opensearch/sql/api/SimpleTable.java` — use it directly, no testFixtures dependency needed
- Spotless uses Google Java Format 1.32.0 with Apache 2.0 license header — run `./gradlew :api:spotlessApply` before build
- JaCoCo enforces 90% coverage on api module — new main source code needs tests or must be covered by existing tests
- Same-package classes in testFixtures can reference main source set classes without explicit imports (both are `org.opensearch.sql.api`)
- Build command: `./gradlew :api:build` runs compile + test + jacoco + spotless check
- Jackson (ObjectMapper, databind) is available transitively in cli via cli → api → ppl → core — no explicit dependency needed
- Jackson deserializes JSON integers as Integer or Long — cast Long to int via .intValue() when mapping to SqlTypeName.INTEGER
- hr.json dataset: employees (id, name, age, department, salary) × 4 rows; departments (id, name, budget) × 3 rows
- Query execution flow: UnifiedQueryContext.builder().language(QueryType).catalog(name, schema).defaultNamespace(name).build() → new UnifiedQueryPlanner(ctx).plan(query) → new UnifiedQueryCompiler(ctx).compile(plan) → stmt.executeQuery()
- SyntaxCheckException is at org.opensearch.sql.common.antlr.SyntaxCheckException — catch it for user-friendly syntax error messages
- UnifiedQueryContext is AutoCloseable — always close before rebuilding or on exit
- For testing CLI classes that need real query execution, use SampleDataLoader.loadFromClasspath("data/hr.json") to get tables, then construct the context/planner/compiler directly
- Package-private methods (loop, dispatch, prompt) enable unit testing of REPL logic without a real JLine terminal
- Gradle `application` plugin `run` task defaults workingDir to subproject dir — use `run { workingDir = rootProject.projectDir }` for root-relative paths
- Gradle `test` task also defaults workingDir to subproject dir — add `workingDir = rootProject.projectDir` to test block if tests use root-relative paths
- SampleDataLoader.loadFile(String) is the unified entry point for file loading — detects .json vs text by extension, derives table name from filename stem
- `.load <path> as <name>` syntax in QueryRepl only affects non-JSON files; JSON files use their own top-level keys as table names
- TIMESTAMP_ISO8601 grok pattern causes duplicate key error in ParseFunction — use %{DATA:fieldname} for timestamps instead
- Grok square bracket escaping: \[ and \] in grok pattern, "\\[" and "\\]" in Java strings
- Grok works end-to-end through Calcite: plan() → compile() → executeQuery() — extracted fields usable in where/stats/sort
- Patterns command works end-to-end through Calcite: uses REGEXP_REPLACE_3 (not ParseFunction UDF), default field `patterns_field`, configurable via `new_field='name'`
- UnifiedQueryContext.Builder.setting(key, value) configures PPL settings — use full key strings like "plugins.ppl.pattern.method"
- LogFormat registry at cli/src/main/java/org/opensearch/sql/cli/LogFormat.java — extensible via LogFormat.register(). Built-in: "opensearch-log" with 5 columns (timestamp, level, component, node, message)
- SampleDataLoader.loadFile(path, format) overload accepts optional format name for structured parsing during load time
- QueryRepl .load supports `--format <name>` flag: `.load <path> [as <name>] [--format <format>]`
- Jackson ObjectMapper.writeValue(OutputStream) closes the stream by default — MUST set `mapper.configure(JsonGenerator.Feature.AUTO_CLOSE_TARGET, false)` when writing multiple JSON values to the same stream (NDJSON/script mode)
- System.console() returns null when stdin is piped — use for auto-detecting non-interactive/script mode

## 2026-03-26T23:25 - US-001
- Promoted SimpleTable from protected static inner class in UnifiedQueryTestBase to public top-level class in api main source set
- Files changed:
  - NEW: api/src/main/java/org/opensearch/sql/api/SimpleTable.java
  - MODIFIED: api/src/testFixtures/java/org/opensearch/sql/api/UnifiedQueryTestBase.java (removed inner class + unused imports)
- **Learnings for future iterations:**
  - SimpleTable was the only inner class in UnifiedQueryTestBase; all test subclasses use it indirectly via createEmployeesTable()
  - No test files needed modification — same-package resolution handles the import automatically
  - The `Schema` import was only needed for `Schema.TableType` in SimpleTable, not in UnifiedQueryTestBase itself
  - 13 imports were removed from UnifiedQueryTestBase (lombok, calcite types only used by SimpleTable)
---

## 2026-03-26T23:31 - US-002
- Created cli Gradle module with JLine3 and PicoCLI dependencies
- Files changed:
  - MODIFIED: settings.gradle (added `include 'cli'`)
  - NEW: cli/build.gradle (java-library, lombok, jacoco, spotless, application plugins; depends on :api, jline 3.28.0, picocli 4.7.6)
  - NEW: cli/src/main/java/org/opensearch/sql/cli/Main.java (minimal entry point printing "OpenSearch Unified Query Shell")
- **Learnings for future iterations:**
  - Plugin versions are NOT specified in subproject build.gradle — they come from root buildscript
  - Version variables like `${hamcrest_version}` and `${mockito_version}` are defined in root build.gradle buildscript.ext and available to all subprojects
  - JaCoCo minimum set to 0.0 for cli since no tests exist yet — increase when tests are added
  - The `application` plugin provides `mainClass` config and `:cli:run` task for running the CLI
  - cli module is NOT in publishedModules list (it's a tool, not a library)
  - JUnit 4 is the default test framework (matching api module pattern); core uses JUnit 5
---

## 2026-03-26T23:35 - US-003
- Implemented SampleDataLoader with load(InputStream) and loadFromClasspath(String) static methods
- Created hr.json dataset with 4 employees and 3 departments at both cli/src/main/resources/data/hr.json and cli/examples/hr.json
- Created SampleDataLoaderTest with 3 tests: testLoadHrDataset, testLoadFromInputStream, testTypeInference
- Files changed:
  - NEW: cli/src/main/java/org/opensearch/sql/cli/SampleDataLoader.java
  - NEW: cli/src/main/resources/data/hr.json
  - NEW: cli/examples/hr.json
  - NEW: cli/src/test/java/org/opensearch/sql/cli/SampleDataLoaderTest.java
- **Learnings for future iterations:**
  - Jackson ObjectMapper is available transitively via cli -> api -> ppl -> core (core declares jackson-core, jackson-databind, jackson-annotations as 'api' scope) — no need to add explicit Jackson dependency to cli/build.gradle
  - Jackson deserializes JSON integers as Integer or Long depending on magnitude — for INTEGER SqlTypeName columns, must handle Long→Integer conversion via .intValue()
  - Type inference from first row: Integer→INTEGER, Long (fits int)→INTEGER, Long→BIGINT, Double→DOUBLE, Boolean→BOOLEAN, String/null→VARCHAR, nested objects/arrays→skip column with stderr warning
  - SampleDataLoader uses private constructor pattern (not @UtilityClass) for utility class
  - hr.json employees have fields: id(int), name(string), age(int), department(string), salary(double)
  - hr.json departments have fields: id(int), name(string), budget(double)
---

## 2026-03-26T23:41 - US-004
- Implemented ResultSetFormatter utility class with format(ResultSet, PrintStream) method
- ASCII table output: header row, separator line, data rows, row count
- Column widths computed from max of header and data value lengths; NULL displayed as "NULL"
- Created ResultSetFormatterTest with 3 tests: basic table, null values, empty result set (all using Mockito mocks)
- Files changed:
  - NEW: cli/src/main/java/org/opensearch/sql/cli/ResultSetFormatter.java
  - NEW: cli/src/test/java/org/opensearch/sql/cli/ResultSetFormatterTest.java
- **Learnings for future iterations:**
  - ResultSetFormatter follows same utility class pattern as SampleDataLoader (private constructor, static methods)
  - ASCII table format: "| col1 | col2 |" header, "+-....-+-....-+" separator, same format for data rows
  - Mockito's when().thenReturn(val1, val2, ...) varargs works well for simulating multiple rs.next()/rs.getObject() calls
  - ByteArrayOutputStream + PrintStream is the standard pattern for capturing output in tests
---

## 2026-03-26T23:45 - US-005
- Implemented QueryRepl.java — JLine3-based interactive REPL with meta-commands and query execution
- Meta-commands: .quit/.exit, .help, .language sql|ppl, .tables, .schema <table>, .load <path>
- Query execution: plan() → compile() → executeQuery() → ResultSetFormatter pipeline
- Error handling: SyntaxCheckException shows "Syntax error: ...", other exceptions show cause message
- Ctrl+C continues loop, Ctrl+D exits, .quit/.exit throws EndOfFileException to exit loop
- Created QueryReplTest with 13 tests covering all meta-commands, query execution, and error cases
- Files changed:
  - NEW: cli/src/main/java/org/opensearch/sql/cli/QueryRepl.java
  - NEW: cli/src/test/java/org/opensearch/sql/cli/QueryReplTest.java
- **Learnings for future iterations:**
  - QueryRepl exposes loop(LineReader) and dispatch(String) as package-private for unit testing without a real terminal
  - rebuildContext() closes old context before creating new one — called on construction, language switch, and data load
  - UnifiedQueryContext is AutoCloseable — must close on REPL exit and before rebuilding
  - AbstractSchema anonymous class wrapping the mutable tables map allows .load to swap data at runtime
  - .quit/.exit throw EndOfFileException (JLine3 convention) to break the loop
  - prompt() returns "ppl> " or "sql> " based on current language
  - Default catalog name "catalog" — tables are referenced as catalog.tablename in queries
  - JavaTypeFactoryImpl() needed for getRowType() calls on Table instances (for .tables and .schema commands)
---

## 2026-03-26T23:55 - US-006
- Implemented Main.java with PicoCLI @Command, Callable<Integer> interface
- Added @Option flags: --data/-d (JSON file path), --language/-l (sql|ppl, default ppl), --execute/-e (single query mode), --help/-h (via mixinStandardHelpOptions)
- Non-interactive mode (-e): loads data, dispatches query via QueryRepl, exits 0 on success / 1 on error
- Interactive mode: prints welcome banner with table names and row counts, enters QueryRepl.run() loop
- Row counting via ScannableTable.scan(null).enumerator() iteration
- Created MainTest with 4 tests: testNonInteractivePpl, testNonInteractiveSql, testNonInteractiveBadQuery, testHelpFlag
- Files changed:
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/Main.java (full rewrite with PicoCLI)
  - NEW: cli/src/test/java/org/opensearch/sql/cli/MainTest.java
- **Learnings for future iterations:**
  - PicoCLI mixinStandardHelpOptions = true adds --help and --version automatically
  - System.out/System.err capture in tests: save originals, set ByteArrayOutputStream wrappers in @Before, restore in @After
  - CommandLine(new Main()).execute(args) returns exit code directly — good for test assertions
  - ScannableTable.scan(null) works for SimpleTable (DataContext not needed) — use enumerator() to count rows
  - QueryRepl.dispatch() is package-private but accessible from MainTest in same package — useful for non-interactive mode
---

## 2026-03-26T23:59 - US-007
- Implemented QueryCompleter.java implementing JLine3 Completer interface with SQL/PPL keyword completion and table/column name completion
- Wired QueryCompleter into QueryRepl.java's LineReader via LineReaderBuilder.completer()
- Added updateLanguage() and updateTables() methods called from switchLanguage() and loadData() in QueryRepl
- Created QueryCompleterTest.java with 10 tests covering SQL/PPL keywords, table/column names, language switching, table updates, case-insensitive matching
- Files changed:
  - NEW: cli/src/main/java/org/opensearch/sql/cli/QueryCompleter.java
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/QueryRepl.java (added completer field, wired into LineReader, update calls in switchLanguage/loadData)
  - NEW: cli/src/test/java/org/opensearch/sql/cli/QueryCompleterTest.java
- **Learnings for future iterations:**
  - JLine3 Completer.complete() receives LineReader, ParsedLine, and List<Candidate> — add matching Candidate objects to the list
  - ParsedLine.word() gives the current word being typed, use it for prefix matching
  - Case-insensitive prefix matching: candidate.toLowerCase().startsWith(prefix.toLowerCase())
  - QueryCompleter stores keyword sets as List<String> and rebuilds them on updateLanguage()
  - Table/column names extracted via table.getRowType(new JavaTypeFactoryImpl()).getFieldNames()
  - Mockito mock of ParsedLine: mock word() and wordCursor() for testing complete()
---

## 2026-03-27T00:06 - US-008
- Implemented QueryHighlighter.java implementing JLine3 Highlighter interface with character-by-character scanning
- Highlighting rules: SQL/PPL keywords → bold+blue, string literals (single-quoted) → green, numbers → magenta, meta-commands (.prefix) → cyan
- Contains same SQL_KEYWORDS (50) and PPL_KEYWORDS (37) lists as QueryCompleter
- Has updateLanguage() and updateTables() methods for dynamic updates; setErrorPattern/setErrorIndex are no-ops
- Wired into QueryRepl: added highlighter field, .highlighter(highlighter) in LineReaderBuilder, update calls in switchLanguage() and loadData()
- Created QueryHighlighterTest with 7 tests: SQL keyword highlighting, PPL keyword highlighting, string literal highlighting, number highlighting, meta-command highlighting, language switching, output length preservation
- Files changed:
  - NEW: cli/src/main/java/org/opensearch/sql/cli/QueryHighlighter.java
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/QueryRepl.java (added highlighter field + wiring)
  - NEW: cli/src/test/java/org/opensearch/sql/cli/QueryHighlighterTest.java
- **Learnings for future iterations:**
  - JLine3 Highlighter.highlight() must return AttributedString with EXACT same length as input buffer — critical for cursor positioning
  - AttributedStringBuilder.styled(style, text) is the cleanest way to append styled segments
  - Character-by-character scanning handles edge cases (adjacent tokens, strings with spaces) better than regex tokenization
  - For testing styles, create a reference AttributedString with known style and compare styleAt().getStyle() values
  - JLine3 Highlighter interface also requires setErrorPattern(Pattern) and setErrorIndex(int) — implement as no-ops
---

## 2026-03-27T00:12 - US-009
- Created cli/examples/ecommerce.json with orders (5 rows), products (5 rows), customers (3 rows)
- Fixed Gradle `run` task working directory — added `workingDir = rootProject.projectDir` to cli/build.gradle so relative paths like `cli/examples/ecommerce.json` resolve correctly from project root
- Verified all 4 acceptance criteria end-to-end:
  - PPL non-interactive: `source = catalog.employees | where age > 30` → Bob (35), Charlie (45) ✓
  - SQL non-interactive: `SELECT * FROM catalog.employees WHERE age > 30` → Bob (35), Charlie (45) ✓
  - Custom data file: `-d cli/examples/ecommerce.json -l sql -e 'SELECT * FROM catalog.customers'` → Alice, Bob, Charlie ✓
  - Build and tests pass ✓
- Files changed:
  - NEW: cli/examples/ecommerce.json
  - MODIFIED: cli/build.gradle (added `run { workingDir = rootProject.projectDir }`)
- **Learnings for future iterations:**
  - Gradle `application` plugin's `run` task defaults working directory to the subproject directory (e.g., `cli/`), not the root project — add `run { workingDir = rootProject.projectDir }` to fix relative path resolution
  - ecommerce.json uses boolean type (in_stock) which exercises BOOLEAN type inference in SampleDataLoader
---

## Round Summary (max iterations reached, iteration 31)

### Progress
- **Completed this round**: All 9 user stories passed (US-001 through US-009)
  - US-001: Promoted SimpleTable to api main source set as public class
  - US-002: Created cli Gradle module with JLine3, PicoCLI, application plugin
  - US-003: Implemented SampleDataLoader with JSON→Table mapping, type inference, hr.json dataset
  - US-004: Implemented ResultSetFormatter with ASCII table output, NULL handling, row counts
  - US-005: Implemented QueryRepl with JLine3 REPL loop, 6 meta-commands, query execution pipeline
  - US-006: Implemented Main entry point with PicoCLI flags (--data, --language, --execute, --help), interactive + non-interactive modes
  - US-007: Added QueryCompleter with SQL/PPL keyword + table/column name tab completion
  - US-008: Added QueryHighlighter with keyword/string/number/meta-command syntax highlighting
  - US-009: Created ecommerce.json dataset, verified all 4 end-to-end acceptance criteria
- **Remaining**: None — all stories complete and passing
- **Blockers**: None. The loop used 31 iterations for 9 stories (~3.4 iterations per story average), suggesting some stories required retry cycles for spotless/build fixes but nothing was fundamentally blocked.

### PoC Direction Check
- **Approach validation**: The approach is fully viable. Calling UnifiedQueryPlanner/UnifiedQueryCompiler directly from a JLine3 REPL works cleanly. The plan→compile→executeQuery→format pipeline is straightforward and the Calcite-backed engine handles both SQL and PPL without issues. SimpleTable as a public API class is a clean abstraction for in-memory data.
- **Surprises**: 
  - Easier than expected: Jackson being transitively available via Calcite eliminated a dependency concern. Same-package resolution between testFixtures and main source set meant US-001 required zero test modifications.
  - Harder than expected: Gradle `application` plugin defaults `workingDir` to the subproject directory, not root — caused relative path issues in US-009 that needed a build.gradle fix. Spotless formatting likely caused retry cycles across multiple stories.
- **Alternative approaches**: None surfaced. The JLine3 + PicoCLI + direct API call approach proved clean. No indication that an alternative architecture would be better for this PoC scope.
- **Risk areas**: 
  - JaCoCo coverage threshold is 0.0 for cli module — if this moves beyond PoC, coverage enforcement should be raised.
  - The highlighter and completer maintain their own keyword lists separate from the actual parser grammars — these will drift as SQL/PPL syntax evolves.
  - No integration test for the interactive REPL path (only unit tests with mocked LineReader) — terminal-dependent behavior could have edge cases.
- **Open questions**: 
  - Should SimpleTable remain in the api module long-term, or should it move to a shared module if other consumers emerge?
  - Should the CLI be distributed as a fat JAR / native image for easier end-user consumption?
  - Should keyword lists be extracted from the ANTLR grammars rather than hardcoded?

### Recommended Next Steps
- Raise JaCoCo coverage threshold for cli module from 0.0 to a reasonable level (e.g., 80%)
- Consider extracting SQL/PPL keyword lists from ANTLR grammar files to keep completer/highlighter in sync with parser
- If shipping beyond PoC: add shadow/fat JAR task or GraalVM native-image build for standalone distribution
- Add a brief README.md to cli/ with usage examples and screenshots
- Consider adding multi-line query support (semicolon-terminated) to the REPL

---

## Human Intervention (2026-03-30)

### New User Stories Added: US-010 through US-020

Added 11 new user stories to prd.json for pre-intern PoC validation. These extend the CLI to support local file analysis, log parsing, visualization, and AI agent cooperation.

**Priority order (do P0 first, then P1, then P2):**

| Priority | Story | Title |
|---|---|---|
| P0 | US-010 | Load raw text files as line-per-row tables |
| P0 | US-011 | Multi-line log joining for stack traces |
| P0 | US-013 | Validate grok command through Unified Query API |
| P1 | US-012 | CSV file support via Calcite CsvSchema |
| P1 | US-014 | Validate patterns command through Unified Query API |
| P1 | US-017 | Query timing and row count display |
| P1 | US-018 | Inline bar chart display for numeric aggregations |
| P1 | US-019 | Machine-readable JSON output for agent cooperation |
| P2 | US-015 | Pre-built format support for opensearch-log |
| P2 | US-016 | Context-aware tab completion from Calcite schema |
| P2 | US-020 | Script mode for multi-command agent pipelines |

**Key context for implementation:**
- US-010/011 are foundation — all log analysis features depend on loading text files
- US-013 is CRITICAL validation — if grok doesn't work through UnifiedQueryPlanner/Compiler, it's a blocker. GrokExpression is in core/, grok patterns in common/src/main/resources/patterns/
- US-015 depends on US-010 + US-011 + US-013
- US-019/020 are for AI agent cooperation — JSON output must be clean (no banners/noise)
- US-018 uses Unicode block chars (█ U+2588) for bar charts in ResultSetFormatter

**Reference docs (on local machine, not in repo):**
- `~/.kiro/plans/opensearch-sql/2026-03-26-cli-implementation-plan.md` — full implementation details for Steps 10-20
- `~/.kiro/plans/opensearch-sql/2026-03-30-intern-cli-playground-plan.md` — intern project plan with demos
- `~/.kiro/plans/opensearch-sql/2026-03-30-cli-feature-gap-analysis.md` — feature gap analysis across 11 CLIs

**Test data needed:**
- Create a sample opensearch.log file at cli/examples/opensearch.log with realistic Log4j2 format lines including at least one multi-line stack trace
- Create a sample CSV at cli/examples/orders.csv for US-012

## 2026-03-31T01:00 - US-010
- Implemented text file loading as line-per-row tables with single VARCHAR 'line' column
- Added SampleDataLoader.loadTextFile(InputStream, String) and loadFile(String) with file extension detection
- Updated QueryRepl .load command to support `.load <path> as <name>` syntax for explicit table naming
- Updated Main.java to use SampleDataLoader.loadFile() for --data/-d flag (auto-detects file type)
- Created cli/examples/opensearch.log with realistic Log4j2 format lines including multi-line stack trace
- Fixed test working directory: added `workingDir = rootProject.projectDir` to cli/build.gradle test block (tests using relative paths like `cli/examples/...` were failing because Gradle test task defaults to subproject dir)
- Added 4 new tests: testLoadTextFile, testLoadTextFileEmptyLines, testLoadFileDetectsJson, testLoadFileDetectsText
- Added 2 new QueryReplTest tests: testLoadTextFile, testLoadTextFileWithAlias
- Verified end-to-end: `-d cli/examples/opensearch.log -e 'source = catalog.opensearch | head 5'` shows 5 log lines
- Files changed:
  - MODIFIED: cli/build.gradle (added workingDir to test block)
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/SampleDataLoader.java (added loadTextFile, loadFile, deriveTableName)
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/QueryRepl.java (added .load as <name> parsing, loadData with alias)
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/Main.java (use loadFile instead of load)
  - MODIFIED: cli/src/test/java/org/opensearch/sql/cli/SampleDataLoaderTest.java (4 new tests)
  - MODIFIED: cli/src/test/java/org/opensearch/sql/cli/QueryReplTest.java (2 new tests)
  - NEW: cli/examples/opensearch.log
- **Learnings for future iterations:**
  - Gradle test task defaults workingDir to subproject dir (e.g., cli/) — must add `workingDir = rootProject.projectDir` to test block if tests use root-relative paths like `cli/examples/...`
  - SampleDataLoader.loadFile(String) is the unified entry point for file loading — detects .json vs everything else by extension
  - deriveTableName() extracts filename stem (e.g., "opensearch" from "opensearch.log") for auto-naming text tables
  - The `.load <path> as <name>` syntax only affects non-JSON files (JSON files have their own table names from top-level keys)
---

## 2026-03-31T01:19 - US-011
- Implemented multi-line log joining for .log files in SampleDataLoader
- Added `loadLogFile(InputStream, String)` method: lines not starting with '[' are appended to previous entry with '\n' separator
- Updated `loadFile` to route .log extension through `loadLogFile` (previously all non-JSON went to `loadTextFile`)
- Added unit test `testLoadLogFileJoinsMultiLineStackTrace`: ERROR + 3 stack trace lines → 1 row containing all 4 lines
- opensearch.log: 10 raw lines → 8 logical entries after joining
- Files changed:
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/SampleDataLoader.java (added loadLogFile, updated loadFile routing)
  - MODIFIED: cli/src/test/java/org/opensearch/sql/cli/SampleDataLoaderTest.java (added testLoadLogFileJoinsMultiLineStackTrace)
- **Learnings for future iterations:**
  - loadFile now has 3-way routing: .json → load(), .log → loadLogFile(), everything else → loadTextFile()
  - loadLogFile is public static like loadTextFile — can be called directly from tests or QueryRepl if needed
  - Multi-line joining pattern: `line.startsWith("[")` detects new log entries; continuation lines get `entries.set(last, last + "\n" + line)`
  - The existing testLoadFileDetectsText test still passes because it only checks the key exists, not row count
---

## 2026-03-31T01:24 - US-013
- VALIDATED: PPL grok command works end-to-end through UnifiedQueryPlanner → UnifiedQueryCompiler → executeQuery
- Created GrokValidationTest.java with 3 tests:
  - testGrokExtractsFieldsFromLogLines: loads opensearch.log, grok extracts ts/level/component/node/msg, verifies 9 rows with all fields non-null
  - testGrokWithWhereFilter: grok + where level = 'ERROR' returns exactly 1 ERROR entry
  - testGrokWithStatsAggregation: grok + stats count() by level verifies INFO=5 and multiple levels
- Grok pattern used: \[%{DATA:ts}\]\[%{LOGLEVEL:level}%{SPACE}\]\[%{DATA:component}\] \[%{DATA:node}\] %{GREEDYDATA:msg}
- Files changed:
  - NEW: cli/src/test/java/org/opensearch/sql/cli/GrokValidationTest.java
- **Learnings for future iterations:**
  - TIMESTAMP_ISO8601 grok pattern CANNOT be used — it expands to sub-patterns (YEAR, MONTHNUM, MONTHDAY, HOUR, MINUTE, SECOND) that create duplicate named group keys, causing Collectors.toMap duplicate key error in ParseFunction.ParseImplementor.parse(). Use %{DATA:ts} instead.
  - Grok pipeline in Calcite: Parse(GROK) AST → CalciteRelNodeVisitor.visitParse() → buildParseRelNode() → GROK UDF RexCall + ITEM accessor for each named group → LogicalProject
  - Grok is implemented as a Calcite UDF (ParseFunction) that returns Map<String,String>, with ITEM operator extracting individual fields
  - Square brackets in grok patterns need escaping: \[ and \] (in Java string: "\\[" and "\\]")
  - LOGLEVEL, DATA, GREEDYDATA, SPACE grok patterns all work correctly through the Calcite pipeline
  - Grok + where filter and grok + stats aggregation both work — extracted fields are fully usable in downstream PPL commands
---
## 2026-03-31T01:38 - US-012
- Implemented CSV file loading with header-based schema inference and type detection
- Added loadCsvFile(), inferCsvType(), convertCsvValue(), stripQuotes() to SampleDataLoader
- Updated loadFile() to dispatch .csv extension to loadCsvFile()
- Updated QueryRepl.loadData() to handle .csv files with alias support (also fixed .log alias path)
- Created cli/examples/orders.csv with 6 rows covering INTEGER, VARCHAR, DOUBLE, BOOLEAN types
- Added 3 tests: testLoadCsvFile, testLoadCsvFileTypeInference, testLoadFileDetectsCsv
- End-to-end verified: PPL stats count() by customer and SQL SUM(price) GROUP BY customer both work on CSV data
- Files changed:
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/SampleDataLoader.java (added loadCsvFile + helpers, updated loadFile)
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/QueryRepl.java (updated loadData alias dispatch for .csv and .log)
  - MODIFIED: cli/src/test/java/org/opensearch/sql/cli/SampleDataLoaderTest.java (3 new tests)
  - NEW: cli/examples/orders.csv
- **Learnings for future iterations:**
  - CSV type inference is string-based (Integer.parseInt, Double.parseDouble, Boolean check) unlike JSON type inference which uses instanceof
  - Simple comma-split CSV parsing works for basic files; quoted fields with embedded commas would need a proper parser
  - QueryRepl.loadData() alias path now properly dispatches .csv and .log files (previously all non-JSON aliases went to loadTextFile)
---

## 2026-03-31T01:50 - US-014
- VALIDATED: PPL patterns command works end-to-end through UnifiedQueryPlanner → UnifiedQueryCompiler → executeQuery
- Created PatternsValidationTest.java with 3 tests:
  - testPatternsExtractsPatternFromLogLines: loads opensearch.log, `patterns line`, verifies patterns_field column with `<*>` replacements across 9 rows
  - testPatternsWithCustomNewField: `patterns line new_field='log_pattern'`, verifies custom field name `log_pattern` is used
  - testPatternsAfterGrok: chains grok → patterns on extracted msg field, verifies patterns_field produced from msg column (9 rows)
- Settings configured via UnifiedQueryContext.Builder.setting() with full key strings (plugins.ppl.pattern.method, etc.)
- Files changed:
  - NEW: cli/src/test/java/org/opensearch/sql/cli/PatternsValidationTest.java
- **Learnings for future iterations:**
  - Patterns command uses REGEXP_REPLACE_3 in Calcite path (not ParseFunction UDF like grok) — replaces [a-zA-Z0-9]+ with `<*>`
  - Default output field name is `patterns_field`; customizable via `new_field='name'` parameter
  - Patterns has its own AST node (Patterns) and visitor (visitPatterns) in CalciteRelNodeVisitor, separate from Parse/visitParse
  - For simple_pattern method, visitPatterns internally creates a Parse node and delegates to buildParseRelNode
  - UnifiedQueryContext.Builder.setting() accepts full key strings like "plugins.ppl.pattern.method" for configuring pattern settings
  - Chaining grok → patterns works: extract fields with grok, then apply patterns to an extracted field
---

## 2026-03-31T01:58 - US-015
- Implemented pre-built format support for opensearch-log with structured column extraction during load time
- Created LogFormat.java: format registry mapping format names to regex Pattern + column definitions
- Added SampleDataLoader.loadFormattedLogFile(): multiline joining → regex parsing → SimpleTable with structured columns
- Added SampleDataLoader.loadFile(path, format) overload for format-aware file loading
- Updated QueryRepl .load command: `.load <path> [as <name>] [--format <format>]` syntax
- Updated Main.java: added `--format/-f` PicoCLI option, passed to SampleDataLoader.loadFile()
- opensearch-log format extracts 5 VARCHAR columns: timestamp, level, component, node, message
- Regex pattern uses DOTALL flag for multiline messages (stack traces joined by US-011)
- Non-matching entries get raw text in message column only (graceful degradation)
- End-to-end verified: `-d cli/examples/opensearch.log --format opensearch-log -e 'source = catalog.opensearch | where level = "ERROR" | stats count() by component'` → 1 row with o.o.a.b.TransportBulkAction
- Files changed:
  - NEW: cli/src/main/java/org/opensearch/sql/cli/LogFormat.java
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/SampleDataLoader.java (loadFormattedLogFile, loadFile overload)
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/QueryRepl.java (--format parsing in .load, 3-arg loadData)
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/Main.java (--format/-f option)
  - MODIFIED: cli/src/test/java/org/opensearch/sql/cli/SampleDataLoaderTest.java (3 new tests)
  - MODIFIED: cli/src/test/java/org/opensearch/sql/cli/QueryReplTest.java (1 new test)
- **Learnings for future iterations:**
  - LogFormat registry is extensible: LogFormat.register(new LogFormat(name, pattern, columns)) adds new formats
  - opensearch-log regex: `\[([^\]]+)\]` captures bracket-delimited fields, `\s*` trims trailing spaces in level/component/node
  - Pattern.DOTALL is essential for multiline-joined entries (stack traces contain \n in message field)
  - loadFormattedLogFile reuses the same multiline joining logic as loadLogFile (lines not starting with '[' are continuations)
  - QueryRepl.deriveTableName() duplicates SampleDataLoader's private method — could be refactored to a shared utility
  - The --format flag works in both interactive (.load command) and non-interactive (-d flag) modes
---

## 2026-03-31T02:09 - US-016
- Implemented context-aware tab completion in QueryCompleter using regex-based context detection
- 7 precompiled regex patterns detect: SQL FROM context, PPL source= context, SQL WHERE context, PPL pipe command context (where/eval/fields), PPL stats...by context
- Table context: suggests only table names (PPL adds 'catalog.' prefix)
- Column context: extracts referenced table name from query, suggests only that table's columns
- Fallback: existing keyword + table/column name completion when no specific context detected
- Added 6 new tests: testSqlCompletesTableNamesAfterFrom, testPplCompletesTableNamesAfterSource, testSqlCompletesColumnNamesAfterWhere, testPplCompletesColumnNamesAfterPipeWhere, testPplCompletesColumnNamesAfterStats, testPplCompletesColumnNamesAfterFields
- All 16 QueryCompleter tests pass (10 existing + 6 new)
- Files changed:
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/QueryCompleter.java (added context detection + table/column-specific completion)
  - MODIFIED: cli/src/test/java/org/opensearch/sql/cli/QueryCompleterTest.java (added 6 context-aware tests + fullLine helper overload)
- **Learnings for future iterations:**
  - JLine3 ParsedLine.line() gives full input, ParsedLine.cursor() gives cursor position — use substring(0, cursor) for context before cursor
  - Regex patterns with `$` anchor at end of beforeCursor string detect what the user just typed before pressing tab
  - SQL_FROM_TABLE and PPL_SOURCE_TABLE patterns handle both bare table names and catalog.table references
  - PPL source completion needs 'catalog.' prefix since queries use 'source = catalog.tablename'
  - extractTableName() uses while(m.find()) to get the LAST match — handles queries with multiple FROM clauses
---

## 2026-03-31T02:21 - US-017
- Added query timing display: every query result now shows 'N row(s) returned (Xms)' after the ASCII table
- Added `format(ResultSet, PrintStream, long elapsedMs)` overload to ResultSetFormatter — when elapsedMs >= 0, appends `(Xms)` to status line
- Existing `format(ResultSet, PrintStream)` delegates to 3-arg overload with elapsedMs=-1 (backward compatible, no timing shown)
- In QueryRepl.executeQuery(): `long start = System.nanoTime()` before plan(), `long elapsedMs = (System.nanoTime() - start) / 1_000_000` after executeQuery(), passed to format()
- Added testFormatWithElapsedTime test asserting `1 row(s) returned (42ms)` output
- Existing tests unchanged — 2-arg format() still works, and contains("row(s) returned") matches new format as substring
- Files changed:
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/ResultSetFormatter.java (added 3-arg format overload with timing)
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/QueryRepl.java (added nanoTime timing in executeQuery)
  - MODIFIED: cli/src/test/java/org/opensearch/sql/cli/ResultSetFormatterTest.java (added testFormatWithElapsedTime)
- **Learnings for future iterations:**
  - Backward-compatible overload pattern (2-arg delegates to 3-arg with sentinel value) avoids updating all callers and tests
  - System.nanoTime() / 1_000_000 gives millisecond precision — sufficient for query timing display
  - Timing wraps plan+compile+executeQuery (not format) — format is just I/O, not meaningful to time
  - Existing test assertions using contains("row(s) returned") naturally match the new "N row(s) returned (Xms)" format
---

## 2026-03-31T02:31 - US-018
- Implemented inline bar chart display for numeric aggregation results in ResultSetFormatter
- Added `format(ResultSet, PrintStream, long, boolean)` 4-arg overload; 3-arg delegates with chartEnabled=false (backward compatible)
- Bar chart logic: finds last numeric column (instanceof Number), computes max value, renders Unicode █ (U+2588) bars proportional to max, MAX_BAR_WIDTH=30
- Time-series detection: if exactly 2 columns and all first-column values contain '-' and ':', bar header is 'trend' instead of 'chart'
- Zero/negative/null values get empty bar string
- Added `.chart on/off` meta-command to QueryRepl (default: on), wired chartEnabled into executeQuery() → format()
- Added `.chart on|off` to .help output
- Added 2 new ResultSetFormatterTest tests: testFormatWithBarChart (verifies █ and 'chart' header), testFormatWithBarChartTimeSeries (verifies 'trend' header)
- Added 1 new QueryReplTest test: testChartMetaCommand (verifies .chart on/off/usage)
- Files changed:
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/ResultSetFormatter.java (4-arg format overload, bar chart logic, MAX_BAR_WIDTH constant)
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/QueryRepl.java (chartEnabled field, .chart case in handleMeta, .help update, executeQuery passes chartEnabled)
  - MODIFIED: cli/src/test/java/org/opensearch/sql/cli/ResultSetFormatterTest.java (2 new tests)
  - MODIFIED: cli/src/test/java/org/opensearch/sql/cli/QueryReplTest.java (1 new test)
- **Learnings for future iterations:**
  - 4-arg overload pattern (adding boolean flag) keeps backward compatibility — 3-arg delegates with default false
  - instanceof Number catches Integer, Long, Float, Double, BigDecimal — more robust than checking ResultSetMetaData column types
  - Time-series detection heuristic: checking for '-' and ':' in string values is simple but effective for ISO timestamps
  - Unicode █ (U+2588) renders well in most terminals; Math.max(1, w) ensures at least 1 block for positive values
---

## 2026-03-31T02:39 - US-019
- Implemented machine-readable JSON output for AI agent cooperation
- Added `--output/-o` PicoCLI option to Main.java (values: 'table' default, 'json')
- Added `boolean jsonOutput` field to QueryRepl with backward-compatible 3-arg constructor delegate
- Added `ResultSetFormatter.formatJson(ResultSet, PrintStream)` using Jackson ObjectMapper — outputs JSON array of row objects
- JSON branches in QueryRepl: executeQuery → formatJson, listTables → JSON array with name/columns, showSchema → JSON array with name/type
- Error handling: all error paths output `{"error":true,"type":"<class>","message":"<msg>"}` when jsonOutput=true
- Exit codes: 0=success, 1=query error, 2=file load error (Main.call wraps file loading in try/catch for exit 2)
- `lastQueryFailed` flag in QueryRepl allows Main to check query status without double-printing errors
- Non-interactive JSON mode suppresses all banner/loaded messages — only JSON to stdout
- Added 3 new MainTest tests: testJsonOutputPpl, testJsonOutputError, testJsonOutputFileError
- All 73 cli tests pass, BUILD SUCCESSFUL
- Files changed:
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/Main.java (--output/-o option, JSON error handling, exit codes)
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/QueryRepl.java (jsonOutput field, JSON branches, writeJson/writeJsonError helpers)
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/ResultSetFormatter.java (formatJson method)
  - MODIFIED: cli/src/test/java/org/opensearch/sql/cli/MainTest.java (3 new tests)
- **Learnings for future iterations:**
  - Backward-compatible constructor pattern (3-arg delegates to 4-arg with default false) avoids updating all existing test files
  - Jackson ObjectMapper is available transitively — no new dependency needed
  - `lastQueryFailed` flag pattern: QueryRepl.dispatch() sets it on error, Main.call() checks it for exit code — avoids catching exceptions twice
  - JSON error string escaping: `e.getMessage().replace("\"", "\\\"")` handles quotes in error messages
  - Gradle build hamster banner comes from Gradle, not CLI — CLI JSON output is clean when run directly
---

## 2026-03-31T02:55 - US-020
- Implemented script mode for multi-command agent pipelines
- Added `--script/-s` PicoCLI boolean flag to Main.java; also auto-detects piped stdin via `System.console() == null`
- Script mode reads lines from System.in via BufferedReader, dispatches each via repl.dispatch(), skips blank lines
- .quit/.exit in script mode breaks loop gracefully via EndOfFileException catch
- Added `public void close()` method to QueryRepl for cleanup after script mode
- Updated loadData() in QueryRepl to output JSON status when jsonOutput=true: `{"status":"ok","tables":[...]}` on success, `{"error":true,...}` on failure
- Fixed critical Jackson bug: ObjectMapper.writeValue() was closing the PrintStream (AUTO_CLOSE_TARGET default=true), breaking subsequent JSON writes in NDJSON mode. Fixed in both QueryRepl.writeJson() and ResultSetFormatter.formatJson()
- Added 3 new MainTest tests: testScriptMode, testScriptModeNdjson, testScriptModeErrorResilience
- All 77 cli tests pass, BUILD SUCCESSFUL
- Files changed:
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/Main.java (--script flag, script mode branch)
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/QueryRepl.java (close(), JSON loadData output, AUTO_CLOSE_TARGET fix)
  - MODIFIED: cli/src/main/java/org/opensearch/sql/cli/ResultSetFormatter.java (AUTO_CLOSE_TARGET fix)
  - MODIFIED: cli/src/test/java/org/opensearch/sql/cli/MainTest.java (3 new tests)
- **Learnings for future iterations:**
  - Jackson ObjectMapper.writeValue(OutputStream, value) closes the stream by default (AUTO_CLOSE_TARGET=true) — MUST configure `mapper.configure(JsonGenerator.Feature.AUTO_CLOSE_TARGET, false)` when writing multiple JSON values to the same stream
  - System.console() returns null when stdin is piped — useful for auto-detecting script mode without requiring --script flag
  - EndOfFileException from .quit/.exit must be caught in the script loop to break gracefully (not crash)
  - Script mode always returns exit code 0 — individual command errors are reported inline (NDJSON error objects), not via exit code
---
