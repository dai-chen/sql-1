calcite:
  logical: |
    LogicalSystemLimit(fetch=[10000], type=[QUERY_SIZE_LIMIT])
      LogicalProject(count()=[$1], address_length=[$0])
        LogicalAggregate(group=[{0}], count()=[COUNT()])
          LogicalProject(address_length=[CHAR_LENGTH($2)])
            LogicalFilter(condition=[>(CHAR_LENGTH($2), 0)])
              CalciteLogicalIndexScan(table=[[OpenSearch, opensearch-sql_test_index_account]])
  physical: |
    EnumerableLimit(fetch=[10000])
      EnumerableCalc(expr#0..1=[{inputs}], count()=[$t1], address_length=[$t0])
        EnumerableAggregate(group=[{0}], count()=[COUNT()])
          EnumerableCalc(expr#0=[{inputs}], expr#1=[CHAR_LENGTH($t0)], expr#2=[0], expr#3=[>($t1, $t2)], address_length=[$t1], $condition=[$t3])
            CalciteEnumerableIndexScan(table=[[OpenSearch, opensearch-sql_test_index_account]], PushDownContext=[[PROJECT->[address]], OpenSearchRequestBuilder(sourceBuilder={"from":0,"timeout":"1m","_source":{"includes":["address"]}}, requestedTotalSize=2147483647, pageSize=null, startFrom=0)])
